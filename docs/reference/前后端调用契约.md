# 前后端调用契约（对外契约面清单）

> 状态：考据文档。对象是旧代码 `参考/`（重构的移植来源与验收基线，即旧 `Waiting_refactored/`），**重构前后必须逐项保持不变**；P6 删除旧码后本文是唯一参照，不随重构更新。

# 对外契约面清单（重构必须保持不变）

## 1. 公共 API 导出面

### 1.1 包入口（package.json exports，仓库根 `package.json:9-25`）

| 入口 | development | 发布产物 |
|---|---|---|
| `.` (node) | `./src/node/index.ts` | `./lib/node/index.js` |
| `.` (browser) | `./src/browser/index.ts` | `./lib/browser/index.js` |
| `./shared` | `./src/shared/index.ts` | `./lib/shared/index.js` |

另：`main: ./lib/node/index.js`，`koishi.service.implements: ['installer']`，optional: `console`, `chatluna`。构建入口为 `src/node/index.ts`、`src/browser/index.ts`、`src/shared/index.ts`（`tsdown.config.ts`）。`src/index.ts` 仅是 `export * from './node'` 占位。

### 1.2 `参考/src/node/index.ts` 的导出

| 导出 | 行号 | 说明 |
|---|---|---|
| `export * from '../shared'` | 66 | 转发 shared 全部导出 |
| `export { Installer }` | 68 | installer 服务类（命名导出，installer.ts 内为 `export default`） |
| 类型 `InstallHistoryChange, InstallHistoryEntry, InstallHistoryStatus, InstallLogDetail` | 69 | from `./installer` |
| 类型 `LocalPackageOperation, LocalPackageUpload{ChunkRequest,CommitResult,FinishRequest,Preview,Progress,StartRequest,StartResult}` | 70-79 | from `./local-upload` |
| 类型 `EnvironmentChangeStatus, EnvironmentDependencySnapshot, EnvironmentSnapshotChange, EnvironmentSnapshotPreview, EnvironmentSnapshotSource, EnvironmentSnapshotSummary` | 80-87 | from `./environment` |
| `name = 'market'` | 236 | 插件名（node 与 browser 同名） |
| `inject = ['http']` | 237 | |
| `usage`（镜像地址文档字符串） | 239-256 | |
| `interface Config` + `Config: Schema<Config>` | 266-291, 761-797 | 见第 3 节 |
| `apply(ctx, config)` | 1399 | 主入口；要求 `ctx.loader.writable`，否则仅告警退出 |

模块增强（必须保留的声明合并）：
- `declare module 'koishi'`: `Context.installer: Installer`（194-198 行）
- `declare module '@koishijs/console'`: `Services: { dependencies, registry, registryStatus, marketData }`（200-208 行）+ `Events`（210-233 行，见第 2.2 节）

### 1.3 `参考/src/shared/` 全部导出

`src/shared/index.ts`（本身）：
| 导出 | 行号 |
|---|---|
| `export * from './bundle' / './dependency-source' / './update'` | 4-6 |
| 接口 `RegistryStatus` | 8-16 |
| 接口 `MarketPerformanceSnapshot` | 18-34 |
| 接口 `MarketRouteScore` | 36-49 |
| 接口 `MarketPerformance` | 51-55 |
| 接口 `MarketLookupRequest` | 57-60 |
| 接口 `MarketLookupResult` | 62-66 |
| 接口 `MarketSnapshotRequest` | 68-70 |
| 接口 `MarketSnapshotTransfer` | 72-78 |
| 类型 `MarketSnapshotResponse` | 80 |
| `Events` 声明合并：`market/refresh`, `market/refresh-dependencies`, `market/index`, `market/lookup`；`Services.market: MarketProvider` | 82-95 |
| 抽象类 `MarketProvider extends DataService<Payload>`（含 abstract `collect()`、abstract `getSnapshot()`、可选 `probeInBackground?(reason)`、`start(refresh)`、`prepare()`） | 99-131 |
| 命名空间 `MarketProvider.Payload` | 133-152 |

`src/shared/bundle.ts`：接口 `PluginBundleMember`(4)、`PluginBundleManifest`(12)、`PluginBundleValidation`(18)、`PluginBundleRecordMember`(24)、`PluginBundleRecord`(33)、`BundleInstallMember`(42)、`BundleInstallRequest`(50)、`BundleInstallResult`(57)、`BundleConfigRemoveRequest`(67)、`BundleConfigRemoveResult`(73)；常量 `BUNDLE_KEYWORD = 'market:package'`(79)、`BUNDLE_PACKAGE_RE`(80)、`PLUGIN_PACKAGE_RE`(81)；函数 `isBundlePackageName`(84)、`hasBundleKeyword`(88)、`isBundleLike`(92)、`parseBundleManifest`(96)、`validateBundleManifest`(119)、`getPluginShortname`(170)、`normalizeBundleIdent`(174)、`getBundleGroupIdent`(182)、`getBundleMemberIdent`(186)、`scanSensitiveConfig`(190)。

`src/shared/dependency-source.ts`：类型 `DependencySource`(1)；接口 `DependencySourceInfo`(11)、`DependencySourceOptions`(17)、`DependencySourceState`(24)、`DiscoveredLocalPluginOptions`(33)；函数 `classifyDependencySource`(42)、`classifyRegistryNotFoundDependency`(64)、`reuseConfirmedDependencySource`(75)、`findUnboundLocalDependencies`(85)、`findDependenciesNeedingSourceCheck`(98)、`isLocalDependency`(115)、`shouldIncludeDiscoveredLocalPlugin`(125)、`allRegistryAttemptsNotFound`(130)、`getRegistryAttemptReasons`(134)、`shouldPenalizeRegistryRoute`(143)。

`src/shared/update.ts`：接口 `UpdateIgnoreRule`(3)、类型 `IgnoredUpdates`(10)、接口 `UpdateIgnorePolicy`(12)；函数 `normalizeUpdateIgnoreRule`(19)、`normalizeUpdateIgnoreCount`(25)、`parseUpdateIgnoredPackages`(30)、`isUpdateCheckDisabled`(37)、`getUpdateCandidates`(41)、`isUpdateVersionIgnored`(59)、`getLatestAllowedUpdate`(79)。

`src/node/data.ts`（经 node/index.ts 使用，未被 `export *` 覆盖但被 client 的 store 类型间接消费）：`MarketDataStorePayload` 接口（12-17：`override / updateIgnored / bundleRecords / collapsedGroups`）、函数 `readMarketDataStore`(26)、类 `MarketDataStore extends DataService`（channel `'marketData'`, 48-49 行）。

### 1.4 client 实际 import 的符号（最关键的契约）

**通过包名 `'koishi-plugin-marketn-refactored'`**（必须保留在 node 入口导出）：
- `client/index.ts:3` — `PluginBundleRecord`, `RegistryStatus`
- `client/shared/operations.ts:3` — `RegistryStatus`
- `client/dialogs/install-history/index.vue:108` — `InstallHistoryChange`, `InstallHistoryEntry`, `InstallLogDetail`
- `client/dialogs/environment-versions/index.vue:120-125` — `EnvironmentChangeStatus`, `EnvironmentSnapshotChange`, `EnvironmentSnapshotPreview`, `EnvironmentSnapshotSource`, `EnvironmentSnapshotSummary`
- `client/pages/dependencies/local-package-upload.vue:78` 与 `client/pages/dependencies/use-local-package-upload.ts:2-6` — `LocalPackageUploadCommitResult`, `LocalPackageUploadPreview`, `LocalPackageUploadStartResult`

**通过相对路径 `../src/...`**（必须保留 shared 模块位置或改路径后同步更新）：
- `client/shared/plugin-config.ts:5-16` — update 的 `getUpdateCandidates, isUpdateCheckDisabled, isUpdateVersionIgnored, normalizeUpdateIgnoreCount, normalizeUpdateIgnoreRule, IgnoredUpdates, UpdateIgnoreRule`；dependency-source 的 `isLocalDependency`；并 `export type { IgnoredUpdates, UpdateIgnoreRule }` 再转发
- `client/index.ts:4` — `DependencySource`（dependency-source）
- `client/pages/dependencies/dependencies.vue:130-131` — `isBundlePackageName`, `shouldIncludeDiscoveredLocalPlugin`
- `client/dialogs/install/index.vue:143-144`、`package.vue:270-271` — `isBundlePackageName`, `isLocalDependency`
- `client/dialogs/bundle-install/index.vue:338-344` — `PluginBundleManifest, hasBundleKeyword, parseBundleManifest, scanSensitiveConfig, validateBundleManifest, getBundleGroupIdent`（同块还有 `BUNDLE_KEYWORD`、`BundleInstallMember` 等位于 344 行之前的 import 前缀）
- `client/shared/operations.ts:9-15` — `getBundleGroupIdent, getPluginShortname, isBundlePackageName, parseBundleManifest, PluginBundleManifest, PluginBundleRecord`
- `client/dialogs/bundle-uninstall/index.vue:98` — `getBundleGroupIdent, PluginBundleRecord`
- `client/extensions/version/index.vue:67` — `PluginBundleRecord`
- `client/market/utils.ts:7` — `hasBundleKeyword, isBundlePackageName`
- `client/market/state.ts:4-9` — `MarketLookupRequest, MarketLookupResult, MarketProvider, MarketSnapshotResponse, MarketSnapshotTransfer`（来自 `'../../src/shared'`）

## 2. Koishi 服务与数据通道

### 2.1 DataService 通道（client store 字段）

| 通道名 | 定义处 | payload 类型 | authority | client 消费 |
|---|---|---|---|---|
| `market` | `MarketProvider`（shared/index.ts:105，抽象基类）；node 实现 src/node/market.ts:99；browser 实现 src/browser/market.ts:4 | `MarketProvider.Payload`（registry, data, dataVersion, total, failed, progress, gravatar, stale, error, cached, cachedAt, validatedAt, serverNow, refreshing, loading, debug） | 4 | `store.market`（client/index.ts:142-157，页面 fields: ['market']） |
| `dependencies` | `DependencyProvider`（src/node/deps.ts:7-15） | `Dict<Dependency>`（installer.ts:180-203：request/resolved/workspace/source/local/bound/invalid/latest） | 4 | `store.dependencies` |
| `registry` | `RegistryProvider`（deps.ts:17-25） | `Dict<Dict<Pick<RemotePackage, DependencyMetaKey>>>` | 4 | `store.registry` |
| `registryStatus` | `RegistryStatusProvider`（deps.ts:27-35） | `Dict<RegistryStatus>` | 4 | `store.registryStatus`（client/index.ts:111-127 另做 sweep） |
| `marketData` | `MarketDataStore`（src/node/data.ts:38-49，`immediate: true`） | `MarketDataStorePayload`（override/updateIgnored/bundleRecords/collapsedGroups） | 4 | `store.marketData`（client/shared/plugin-config.ts:106） |

### 2.2 RPC / 事件监听器全集（`ctx.console.addListener`，全部 `{ authority: 4 }`）

注册于 `src/node/index.ts:1573-1707`，另有 `market/refresh` 在 `src/shared/index.ts:107`：

| 事件名 | 参数 | 返回 | 实现 | 注册行 |
|---|---|---|---|---|
| `market/install` | `deps: Dict<string>`, `forced?`, `options?: InstallOptions` | `Promise<number>` | `installer.install` + ensurePluginConfigs + refresh 4 通道 | 1573 |
| `market/install-bundle` | `request: BundleInstallRequest`, `forced?`, `options?` | `Promise<BundleInstallResult>` | `installBundle()`（本地函数） | 1594 |
| `market/install-fallback-candidate` | `failedEndpoint?: string` | `Promise<InstallFallbackCandidate \| undefined>` | `installer.getInstallFallbackCandidate` | 1599 |
| `market/install-history` | `limit?: number` | `Promise<InstallHistoryEntry[]>` | `installer.getInstallHistory` | 1603 |
| `market/install-history-detail` | `id: string` | `Promise<InstallLogDetail \| undefined>` | `installer.getInstallLogDetail` | 1607 |
| `market/local-package-upload-start` | `request: LocalPackageUploadStartRequest` | `Promise<LocalPackageUploadStartResult>` | `installer.startLocalPackageUpload` | 1611 |
| `market/local-package-upload-chunk` | `request: LocalPackageUploadChunkRequest` | `Promise<LocalPackageUploadProgress>` | `installer.appendLocalPackageUpload` | 1615 |
| `market/local-package-upload-finish` | `request: LocalPackageUploadFinishRequest` | `Promise<LocalPackageUploadPreview>` | `installer.finishLocalPackageUpload` | 1619 |
| `market/local-package-upload-commit` | `uploadId: string` | `Promise<LocalPackageUploadCommitResult>` | `installer.commitLocalPackageUpload` | 1623 |
| `market/local-package-upload-cancel` | `uploadId: string` | `Promise<boolean>` | `installer.cancelLocalPackageUpload` | 1627 |
| `market/prepare-local-binding` | `name: string` | `Promise<LocalBindingResult>` | `installer.prepareLocalBinding` | 1631 |
| `market/environment-snapshots` | — | `Promise<EnvironmentSnapshotSummary[]>` | `installer.getEnvironmentSnapshots` | 1635 |
| `market/environment-snapshot-preview` | `id: string` | `Promise<EnvironmentSnapshotPreview \| undefined>` | `installer.getEnvironmentSnapshotPreview` | 1639 |
| `market/environment-snapshot-apply` | `id, options?: InstallOptions` | `Promise<number>` | `installer.applyEnvironmentSnapshot` + refresh | 1643 |
| `market/remove-bundle-configs` | `request: BundleConfigRemoveRequest` | `Promise<BundleConfigRemoveResult>` | 本地 `removeBundleConfigs()` | 1653 |
| `market/update-config` | `patch: Partial<Config>`（白名单键 configPatchKeys 858-877） | `Promise<boolean>` | 本地 `updateMarketNextConfig()`（写 loader 配置） | 1657 |
| `market/update-data` | `patch: Partial<MarketDataStorePayload>` | `Promise<MarketDataStorePayload>` | `dataStore.patch` | 1661 |
| `market/refresh-dependencies` | — | `Promise<void>` | `installer.refresh(true)` + refresh('config') | 1665 |
| `market/package` | `name: string` | `Promise<Registry>` | `installer.getRegistry` | 1670 |
| `market/index` | `request?: MarketSnapshotRequest`（transport: `'inline' \| 'http-gzip'`） | `Promise<MarketSnapshotResponse>` | `services.market.getSnapshot()`；http-gzip 时经 `MarketSnapshotTransport` 返回 `{transport, url, payload, decodedSize, encodedSize}` | 1674 |
| `market/lookup` | `request: MarketLookupRequest`（names/services） | `Promise<MarketLookupResult>` | 本地 `lookupMarket()`（names≤512, services≤128） | 1680 |
| `market/registry` | `names: string[]` | `Promise<Dict<Dict<Pick<RemotePackage, DependencyMetaKey>>>>` | `installer.getPackage` 并发 pMap | 1684 |
| `market/ensure-config` | `name: string` | `Promise<boolean>` | 本地 `ensurePluginConfig()` | 1697 |
| `market/avatar` | `key: string`, `url?: string` | `Promise<AvatarFetchResult \| undefined>`（`{data, type, cached?}`） | 本地 `fetchAvatar()`（内存+磁盘缓存、SSRF 防护） | 1701 |
| `market/refresh` | — | `Promise<void>` | `MarketProvider.start(true)` | shared/index.ts:107 |

client 端调用点核对（全部为 `send(...)`，无 `service()` 调用）：`market/install`(shared/operations.ts:382)、`market/install-bundle`(bundle-install/index.vue:665)、`market/install-fallback-candidate`(utils.ts:294)、`market/install-history`(install-history/index.vue:134)、`market/install-history-detail`(:161)、`market/local-package-upload-*`(pages/dependencies/use-local-package-upload.ts:46,60,71,95,158)、`market/prepare-local-binding`(package.vue:692)、`market/environment-snapshots`(environment-versions/index.vue:176)、`market/environment-snapshot-preview`(:202)、`market/environment-snapshot-apply`(shared/operations.ts:463)、`market/remove-bundle-configs`(confirm/index.vue:92, bundle-uninstall/index.vue:277)、`market/update-config`(client/shared/plugin-config.ts:307)、`market/update-data`(client/shared/plugin-config.ts:318)、`market/refresh-dependencies` / `market/refresh`(client/index.ts:306)、`market/package`(bundle-install/index.vue:465, shared/operations.ts:112,218)、`market/index`(market/state.ts:80,89)、`market/lookup`(market/state.ts:266)、`market/registry`(install/index.vue:372,403, bundle-install/index.vue:509)、`market/ensure-config`(shared/operations.ts:521)、`market/avatar`(market/utils.ts:267,296)。外部通道：`send('manager/remove', path, id)`（extensions/config-remove/index.vue:50，依赖 @koishijs/plugin-manager）。

### 2.3 广播事件（`ctx.console.broadcast` → client `receive()`）

| 事件名 | payload | 发出方 | client 接收方 |
|---|---|---|---|
| `market/patch` | `Partial<MarketProvider.Payload>`（增量 data delta） | node/market.ts:147（throttle 500ms） | market/state.ts:296（合并快照） |
| `market/registry` | `Dict<Dict<Pick<RemotePackage, DependencyMetaKey>>>`（增量） | installer.ts:304（throttle 500ms） | client/index.ts:104（合并进 store.registry） |
| `market/registry-status` | `Dict<RegistryStatus>`（增量） | installer.ts:308（throttle 200ms） | client/index.ts:111 |
| `market/registry-status/clear` | `{}` | installer.ts:767（`as any` 强转） | client/index.ts:124 |
| `market/install-log` | `{ type: 'stdout'\|'stderr', line: string }` | installer.ts:1196 | shared/operations.ts:264 |

### 2.4 HTTP 路由

- `GET {console.uiPath}/market-next/snapshot/:id` — src/node/index.ts:1553-1570，gzip 市场快照（ETag=内容 hash，`Cache-Control: immutable`），配合 `market/index` 的 `http-gzip` transport。

## 3. 命令与配置

### 3.1 ctx.command（均在 `src/node/index.ts`，authority 4）

| 命令 | 别名 | 选项 | 行为 | 行号 |
|---|---|---|---|---|
| `plugin.install <name>` | `.i` | — | resolveName → 查重 → findVersion → install + ensurePluginConfigs，写重启消息 | 1420-1444 |
| `plugin.uninstall <name>` | `.r` | — | resolveName → `install({[name]: null})` | 1446-1459 |
| `plugin.upgrade [name...]` | `.update`, `.up` | `-s,--koishi`(self)、`-f,--force` | refresh(true,true) → getDeps → getUpdateCandidates/getLatestAllowedUpdate（忽略策略）→ 逐个列出版本、prompt 确认 → install | 1461-1527 |
| `plugin.clear-avatar-cache` | — | — | 清空头像内存+磁盘缓存，返回统计 | 1529-1533 |

### 3.2 Config Schema 完整结构（`src/node/index.ts:761-797`，接口 266-291）

```
frontendMode: 'performance'|'polished' (radio, default 'performance')
depsLayout: 'grid'|'list' (radio, default 'grid')
idleProbe: boolean (default true)
idleProbeDelay: number(time, default 5min)
idleProbeBootDelay: number(time, default 1min)
idleProbeInterval: number(time, default 6h)
bulkMode: boolean (hidden, default false)
removeConfig: undefined|true|false (hidden)          # 卸载时是否删插件配置
updateIgnoredPackages: string(textarea, hidden)
updateIgnoreDuration: number(time, hidden, default 0)
updateIgnoreVersions: number(1-20, hidden, default 1)
updateIgnorePrerelease: boolean(hidden, default false)
collapsedGroups: Dict<boolean>(hidden)
registry: Installer.Config
search: MarketProvider.Config
chatlunaTool: boolean (default false)
marketSilentFilters: string(textarea, hidden)        # legacy
marketSilentStatusRules / marketSilentDateRules / marketSilentRecentRules / marketSilentCustomRules: Schema.any[] (hidden)
marketSilentRules: Array<{ type, value, note, enabled }> (table, default [])
```
`marketSilentRules.type` 枚举：`preview / insecure / bundle / created-before / created-after / updated-before / updated-after / created-within / updated-within / custom`（741-752）。

子配置：
- `Installer.Config`（installer.ts:1980-1999）：`endpoint`(link)、`timeout`(time, default 5s)、`autoRoute`(default true)、`retry`(0-5, default 1)、`concurrency`(1-16, default 4)、`installLogRetentionHours`(default 72)；deprecated `installLogRetention`。
- `MarketProvider.Config`（node/market.ts:1582-1596）：`endpoint`(link, default `'https://registry.koishi.t4wefan.pub/index.json'`，即 `DEFAULT_ENDPOINT`)、`timeout`(default 30s)、`proxyAgent`、`autoRoute`(default true)、`logLevel`(`silent|error|warn|info|debug`, default 'warn')。

配置由 `market/update-config` RPC 写回（白名单 `configPatchKeys`，858-877 行）；`idleProbe*` 变更触发插件 reload（`configReloadKeys`，879-884）。

## 4. src/browser 入口（浏览器打包）

`src/browser/index.ts`（30 行）：
- 导出：`export * from './market'`（browser/market.ts 无命名导出）、`export * from '../shared'`、`export { MarketProvider }`（browser 实现）、`filter = false`、`name = 'market'`、`inject = ['console']`、空 `Config`、`apply(ctx)`。
- `apply` 做三件事：(1) `ctx.plugin(MarketProvider)`（browser 版，DataService `'market'`）；(2) 注册 `market/index` 监听器（无参调用 `services.market.getSnapshot()`，不支持 http-gzip）；(3) `ctx.console.addEntry(...)` 指向 `client/index.ts`（dev）或 `KOISHI_BASE/dist`（静态环境）。

`src/browser/market.ts`（23 行）：`MarketProvider extends BaseMarketProvider`，`collect()` 直接返回 `this.ctx.loader.market`（Koishi WebUI 内置 registry 数据），`get()/getSnapshot()` 把 objects 映射为 `{data: {name: SearchObject}, failed:0, total, progress}`。用途：在浏览器/纯前端构建（无 node HTTP）里提供同一套 market 通道与类型。

## 5. Installer 服务 public 方法清单（`src/node/installer.ts`，类声明 244 行，`ctx.installer`）

public 字段：`http`、`endpoint`、`fullCache`、`tempCache`、`registryStatus`、`config`、`ctx`；getter：`cwd`、`isInstalling`。

| 方法 | 签名要点 | 用途一句话 | 行号 |
|---|---|---|---|
| `start()` | — | 服务启动：载入路由统计、清理日志、重置 endpoint、记录启动环境快照、调度元数据探测 | 321 |
| `resolveName(name)` | → `string[]` | 短名 → 候选包名列表（@koishijs/plugin-x 与 koishi-plugin-x） | 415 |
| `findVersion(names)` | → `Promise<Dict<string>>` | 在候选名中找最新可用版本 | 426 |
| `getInstallFallbackCandidate(failedEndpoint?)` | → `InstallFallbackCandidate \| undefined` | 按路由评分推荐备用 npm 源（label/reason） | 688 |
| `getRegistry(name, serial?)` | → `Promise<Registry>` | 带重试/竞速/状态上报的 npm 元数据获取 | 770 |
| `setPackage(name, versions)` | — | 外部注入包版本缓存并广播 | 908 |
| `getPackage(name)` | → `Promise<Dict<Pick<RemotePackage,DependencyMetaKey>> \| undefined>` | 带任务去重与 404 缓存的版本获取 | 914 |
| `refreshDependencyMetadata(wait)` | → `Promise \| undefined` | 后台刷新依赖 latest（幂等） | 1030 |
| `probeDependenciesInBackground(reason)` | — | 空闲探测：重置缓存并完整刷新依赖与市场数据 | 1045 |
| `getDeps(options?)` | → `Dict<Dependency>` / Promise | 依赖快照（可选触发元数据刷新） | 1071 |
| `refreshData()` | — | 刷新 dependencies/registry/registryStatus/packages 四通道 | 1080 |
| `refresh(refresh?, waitMetadata?)` | — | 全量重置依赖缓存与元数据 | 1089 |
| `getInstallHistory(limit=20)` | → `InstallHistoryEntry[]`（clamp 1-50） | 安装日志历史列表 | 1354 |
| `getInstallLogDetail(id)` | → `InstallLogDetail \| undefined` | 单条安装日志内容（head/tail 截断） | 1377 |
| `getEnvironmentSnapshots()` | → `EnvironmentSnapshotSummary[]` | 环境快照列表（标记 current） | 1391 |
| `getEnvironmentSnapshotPreview(id)` | → `EnvironmentSnapshotPreview \| undefined` | 快照与当前环境 diff 预览 | 1399 |
| `exec(args)` | → `Promise<number>` | 运行包管理器（yarn berry JSON 解析、stdout/stderr 逐行广播+落盘） | 1414 |
| `override(deps)` | — | 直接写 package.json dependencies（null=删除，排序） | 1504 |
| `install(deps, forced?, beforeReload?, options?)` | → `Promise<number>` | 排队执行安装（快照/回滚/本地插件保护/fullReload 判定/日志） | 1807 |
| `startLocalPackageUpload(request)` | → `StartResult` | 开启本地 .tgz 分片上传会话 | 1811 |
| `appendLocalPackageUpload(request)` | → `Progress` | 追加 base64 分片（严格顺序/大小校验） | 1815 |
| `finishLocalPackageUpload(request)` | → `Preview` | 完成上传并解包校验，产出预览（operation 等） | 1819 |
| `commitLocalPackageUpload(uploadId)` | → `CommitResult` | 把归档原子移入 `.yarn/local` 并返回 file: 请求串 | 1841 |
| `cancelLocalPackageUpload(uploadId)` | → `boolean` | 取消并清理会话 | 1845 |
| `prepareLocalBinding(name)` | → `LocalBindingResult` | 对 unbound 本地插件执行 `npm pack` 并落地归档 | 1849 |
| `applyEnvironmentSnapshot(id, options?)` | → `Promise<number>` | 按快照恢复依赖环境（拒绝 unsupported 本地依赖） | 1948 |
| `isSelfUpdate(deps)` | → `boolean` | deps 是否包含本插件包名 | 1969 |

命名空间导出：`Installer.GetDepsOptions`（1975）、`Installer.Config` 接口与 Schema（1980-1998）。文件级导出：`InstallOptions`(124)、`InstallFallbackCandidate`(128)、`InstallHistoryStatus`(134)、`InstallHistoryChange`(136)、`InstallHistoryEntry`(144)、`LocalBindingResult`(157)、`InstallLogDetail`(163)、`Dependency`(180)、`YarnLog`(205)、`LocalPackage`(219)、`loadManifest(name, baseDir?)`(224)、`export default Installer`(2095)。

## 其他重构时需注意的隐式契约

- ChatLuna 工具名 `koishi_plugin_market_search`（src/node/chatluna.ts:7，`applyChatLunaTool` 163 行；`ChatLunaToolConfig` 49 行）；依赖 `chatluna.platform.registerTool`。
- 磁盘路径契约：`data/market-next.json`、`data/market-next-environment-snapshots.json`、`data/market-next-install-logs/`、`cache/market-next-avatars/`、`cache/market-next-registry-stats.json`、`cache/market-next-index.json`、`cache/market-next-index/`、`.yarn/local/`（及 `file:.yarn/local/xxx.tgz` 请求串格式）。
- client 注册的 Console 扩展点：页面 `market`(/market, fields:['market'])、`dependencies`(/dependencies, fields:['dependencies','registry','registryStatus'])；slot 类型 `welcome-choice`、`global`(×7)、`status-right`、`plugin-dependency`、`plugin-details`、`plugin-missing`、`plugin-select`；action `market.refresh`(ctrl+r)、`market.install`、`dependencies.manual/history/versions`；menu `market`、`dependencies`；并猴补 `config.tree.remove` action 与 label（extensions/index.ts:20-87）。
- `client/market/index.ts` 是可复用组件库出口：`MarketIcon, MarketFilter, MarketList, MarketPackage, MarketSearch` + `export * from './utils'`（market/utils.ts 的 badges/comparators/categories/getVisible/getSorted/validate 等约 30 个导出）。
- client 页面消费 `store.packages`、`store.registry`、`store.market`、`store.dependencies`、`store.marketData`、`store.registryStatus`；`receive` 处理器共 5 个（见 2.3 表）。
- node/index.ts `apply` 中的插件编排顺序：`applyChatLunaTool` → `ctx.plugin(Installer, config.registry)` → inject(installer) 注册命令 → inject(console,installer,server) 注册 3 个 Provider + MarketDataStore + `ctx.plugin(MarketProvider, config.search)` + addEntry + 快照路由 + 25 个 listener + ready 钩子（dataStore.migrateFromConfig、插件配置修复、头像缓存清扫）。
