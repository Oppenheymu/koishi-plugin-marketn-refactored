# 前后端调用契约（对外契约面清单）

> 状态：考据文档（P6 契约冻结核对后定稿）。对象是旧代码 `参考/`（重构的移植来源与验收基线，即旧 `Waiting_refactored/`）；P6 删除旧码后本文是唯一参照。
>
> **P6 冻结修订说明（2026-08-23）**：本文已经过与当前代码的逐项核对（RPC 25 / 广播 5 / 命令 4 / 通道 5 / 磁盘路径 8 全部一致），实质修订随文标注。
>
> **行号勘误**：`参考/` 在本文初稿后经历了一轮自身拆分（`src/node/index.ts` 约 1700 行 → 226 行 + `console-listeners.ts` / `commands.ts` / `config.ts` / `registry-metadata.ts` / `install-history.ts` / `installer-types.ts` / `chatluna-search.ts` 等约 20 个文件；`client/index.ts` 420 行 → 45 行 + `actions.ts` / `pages.ts` / `registry-state.ts` / `i18n.ts` 等）。除特别注明外，本文行号已更新为拆分后 `参考/` 的实际位置；`参考/src/node/installer.ts` §5 表中的行号对应拆分前的大文件版本（现仅 803 行），检索以符号名为准。
>
> **有意砍除项（重构决策，非遗漏）**：browser 独立入口（§1.1 / §4）、ChatLuna 工具（隐式契约）、`fallbackReason` 的 `primary-stale` 归因与市场索引世代（generation）防回退（附录 B）。

# 对外契约面清单（重构必须保持不变）

## 1. 公共 API 导出面

### 1.1 包入口（package.json exports）

| 入口 | development | 发布产物 |
|---|---|---|
| `.` (node) | `./src/node/index.ts` | `./lib/node/index.cjs` |
| `./shared` | `./src/shared/index.ts` | `./lib/shared/index.cjs` |

另：`main: ./lib/node/index.cjs`、`types: ./lib/node/index.d.ts`，`koishi.service.implements: ['installer']`，optional: `console`。构建入口为 `src/node/index.ts` 与 `src/shared/index.ts` 双入口（`tsdown.config.ts`，`outExtensions` 固定 `.cjs`——Koishi loader 以 `require()` 加载所需）。
修订记录：旧版的 browser 入口（`./src/browser/index.ts` → `./lib/browser/index.js`）已随 browser 独立入口一并砍除；产物扩展名由 `.js` 改为 `.cjs`（CJS 打包格式变更，加载语义不变）；optional 中的 `chatluna` 随 ChatLuna 工具砍除。

### 1.2 `参考/src/node/index.ts` 的导出（226 行版本）

| 导出 | 行号 | 说明 |
|---|---|---|
| `export * from '../shared'` | 50 | 转发 shared 全部导出 |
| `export { Installer }` | 53 | installer 服务类（命名导出，installer.ts 内为 `export default`） |
| 类型 `InstallHistoryChange, InstallHistoryEntry, InstallHistoryStatus, InstallLogDetail` | 55-58 | from `./installer` |
| 类型 `LocalPackageOperation, LocalPackageUpload{ChunkRequest,CommitResult,FinishRequest,Preview,Progress,StartRequest,StartResult}` | 59-67 | from `./local-upload` |
| 类型 `EnvironmentChangeStatus, EnvironmentDependencySnapshot, EnvironmentSnapshotChange, EnvironmentSnapshotPreview, EnvironmentSnapshotSource, EnvironmentSnapshotSummary` | 68-73 | from `./environment` |
| `name = 'market'` | 116 | 插件名 |
| `inject = ['http']` | 117 | |
| `usage`（镜像地址文档字符串，12 个市场镜像 + 论坛链接，见附录 B） | 119-136 | |
| `interface Config` + `Config: Schema<Config>` | 已移至 `参考/src/node/config.ts`（接口 @6、Schema @90） | 见第 3 节 |
| `apply(ctx, config)` | 146 | 主入口；要求 `ctx.loader.writable`，否则仅告警退出 |

模块增强（必须保留的声明合并）：
- `declare module 'koishi'`: `Context.installer: Installer`（74-78 行）
- `declare module '@koishijs/console'`: `Services: { dependencies, registry, registryStatus, marketData }`（80-88 行）+ `Events`（90-114 行，见第 2.2 节）

当前版本差异：`src/node/index.ts` 额外导出 `LocalBindingResult`（参考中它是 installer.ts 的文件级导出、未经包入口转发；当前经入口导出，属契约面扩展，无破坏性）。

### 1.3 `参考/src/shared/` 全部导出

`src/shared/index.ts`（本身）：
| 导出 | 行号 |
|---|---|
| `export * from './bundle' / './dependency-source' / './update'` | 4-6 |
| 接口 `RegistryStatus` | 8-16 |
| 接口 `MarketPerformanceSnapshot` | 18-34 |
| 接口 `MarketRouteScore` | 36-49 |
| 接口 `MarketPerformance` | 51-55 |
| 接口 `MarketLookupRequest` | 57-60 |
| 接口 `MarketLookupResult` | 62-66 |
| 接口 `MarketSnapshotRequest` | 68-70 |
| 接口 `MarketSnapshotTransfer` | 72-78 |
| 类型 `MarketSnapshotResponse` | 80 |
| `Events` 声明合并：`market/refresh`, `market/refresh-dependencies`, `market/index`, `market/lookup`；`Services.market: MarketProvider` | 82-95 |
| 抽象类 `MarketProvider extends DataService<Payload>`（含 abstract `collect()`、abstract `getSnapshot()`、可选 `probeInBackground?(reason)`、`start(refresh)`、`prepare()`） | 99-131 |
| 命名空间 `MarketProvider.Payload` | 133-152 |

`src/shared/bundle.ts`：接口 `PluginBundleMember`(4)、`PluginBundleManifest`(12)、`PluginBundleValidation`(18)、`PluginBundleRecordMember`(24)、`PluginBundleRecord`(33)、`BundleInstallMember`(42)、`BundleInstallRequest`(50)、`BundleInstallResult`(57)、`BundleConfigRemoveRequest`(67)、`BundleConfigRemoveResult`(73)；常量 `BUNDLE_KEYWORD = 'market:package'`(79)、`BUNDLE_PACKAGE_RE`(80)、`PLUGIN_PACKAGE_RE`(81)；函数 `isBundlePackageName`(84)、`hasBundleKeyword`(88)、`isBundleLike`(92)、`parseBundleManifest`(96)、`validateBundleManifest`(119)、`getPluginShortname`(170)、`normalizeBundleIdent`(174)、`getBundleGroupIdent`(182)、`getBundleMemberIdent`(186)、`scanSensitiveConfig`(190)。

`src/shared/dependency-source.ts`：类型 `DependencySource`(1)；接口 `DependencySourceInfo`(11)、`DependencySourceOptions`(17)、`DependencySourceState`(24)、`DiscoveredLocalPluginOptions`(33)；函数 `classifyDependencySource`(42)、`classifyRegistryNotFoundDependency`(64)、`reuseConfirmedDependencySource`(75)、`findUnboundLocalDependencies`(85)、`findDependenciesNeedingSourceCheck`(98)、`isLocalDependency`(115)、`shouldIncludeDiscoveredLocalPlugin`(125)、`allRegistryAttemptsNotFound`(130)、`getRegistryAttemptReasons`(134)、`shouldPenalizeRegistryRoute`(143)。

`src/shared/update.ts`：接口 `UpdateIgnoreRule`(3)、类型 `IgnoredUpdates`(10)、接口 `UpdateIgnorePolicy`(12)；函数 `normalizeUpdateIgnoreRule`(19)、`normalizeUpdateIgnoreCount`(25)、`parseUpdateIgnoredPackages`(30)、`isUpdateCheckDisabled`(37)、`getUpdateCandidates`(41)、`isUpdateVersionIgnored`(59)、`getLatestAllowedUpdate`(79)。

`src/node/data.ts`（经 node/index.ts 使用，未被 `export *` 覆盖但被 client 的 store 类型间接消费）：`MarketDataStorePayload` 接口（12-17：`override / updateIgnored / bundleRecords / collapsedGroups`）、函数 `readMarketDataStore`(26)、类 `MarketDataStore extends DataService`（channel `'marketData'`, 48-49 行）。

**当前版本结构差异（冻结记录）**：
- shared 出口扩为 6 个 `export *`（新增 `./lookup`、`./provider`、`./types`），接口与基类移入 `types.ts` / `provider.ts`。
- `MarketProvider` 基类形状变化：abstract `start(refresh?)`（由具体方法改为抽象）+ abstract `getSnapshot()` + 可选 `probeInBackground?`；`collect()` / `prepare()` 钩子移除（去重与竞速逻辑下沉 `core/market/source/`）。
- bundle.ts 拆分：`getPluginShortname` / `normalizeBundleIdent` / `getBundleGroupIdent` / `getBundleMemberIdent` / `scanSensitiveConfig` 5 个 ident/扫描函数移至 `bundle-idents.ts`，经 shared 出口仍可达（P6 契约核对时补回 `export *`）。
- `MarketProvider.Payload` 与 `MarketLookupResult` 各新增 `revision` 字段（结构修订号，区别于 `dataVersion` 内容版本；向后兼容扩展）。
- `fallbackReason` 枚举由 4 值收缩为 3 值：`'primary-failed' | 'primary-slow' | 'rescue'`（`primary-stale` 随世代防回退一并砍除）。
- 新增超出旧契约的导出：`collectServiceProviders`（lookup.ts）、`InstallFallbackCandidate`（types.ts，旧版在 installer.ts 文件级导出）、`MarketSilentRule` 及 `MarketSilentStatusRule/DateRule/RecentRule/CustomRule` 5 类型（types.ts）。

**shared 纯函数边界行为（P6 补全考据）**：
- `classifyDependencySource` 判定顺序：`file:` / `link:` / `portal:`（yarn 特有）/ `workspace:` 协议 → 本地路径（`./`、`../`、`X:\`、`/`、`\\`）→ `options.workspace` → http/ftp URL → git（`git+..:`、`github:`、`gitlab:`、`bitbucket:` 及 `owner/repo#ref` 简写正则）→ unbound（须 installed 且 discoveredLocal/registryNotFound）→ registry。
- `shouldPenalizeRegistryRoute`：not-found 失败**不**罚分（镜像未同步不该罚镜像）；unbound 判定复用要求 request 与 resolved 均未变。
- `parseUpdateIgnoredPackages` 分隔符正则 `/[\s,，;；]+/g`（含中文逗号/分号），包名 trim + lowercase。
- 忽略规则语义：`until` 过期即失效；目标版本比被忽略版本**新**（candidates 索引更大）直接忽略；否则落后 `count-1` 个内仍忽略（count clamp 1-20，NaN→1）。
- bundle 包名正则 `koishi-plugin-pa-*`（pa 前缀）且必须已是小写；`PLUGIN_PACKAGE_RE` 同时接受 `koishi-plugin-*` 与 `@koishijs/plugin-*`；敏感配置键正则 `/(command|script|exec|shell|path|file|token|secret|password|sql|url|webhook|endpoint)/i`；校验错误全集：非小写、自引用、`package\nplugin` 组合重复 = error，package/plugin 各自重复 = warning、version 须 validRange、plugin 键格式建议 warning；`normalizeBundleIdent` 48 字符截断、空回退 `'bundle'`；组标识 `pa-{短名}`、成员标识 `pa-{bundle}-{member}`。

### 1.4 client 实际 import 的符号（最关键的契约）

注：参考代码 self-import 用旧包名 `'koishi-plugin-market-next'`；当前 client 用新包名 `'koishi-plugin-marketn-refactored'`（见隐式契约"旧包名硬编码点"）。下列文件位置为**当前** client/ 拆分后布局（P6 核对更新）。

**通过包名（必须保留在 node 入口导出）**：
- `client/app/registry-state.ts:16-17` — `PluginBundleRecord`, `RegistryStatus`, `DependencySource`
- `client/shared/operations/analyze.ts` — `RegistryStatus`
- `client/dialogs/install-history/index.vue:122` — `InstallHistoryChange`, `InstallHistoryEntry`, `InstallLogDetail`
- `client/dialogs/environment-versions/index.vue:135-141` — `EnvironmentChangeStatus`, `EnvironmentSnapshotChange`, `EnvironmentSnapshotPreview`, `EnvironmentSnapshotSource`, `EnvironmentSnapshotSummary`
- `client/pages/dependencies/local-package-upload.vue:87` — `LocalPackageUploadPreview`；`client/pages/dependencies/use-local-package-upload.ts:17-20` — `LocalPackageUploadCommitResult`, `LocalPackageUploadPreview`, `LocalPackageUploadStartResult`

**通过相对路径（必须保留 shared 模块位置或改路径后同步更新）**：
- `client/shared/plugin-config/update-policy.ts` — update 的 `getUpdateCandidates, isUpdateCheckDisabled, isUpdateVersionIgnored, normalizeUpdateIgnoreCount, normalizeUpdateIgnoreRule, IgnoredUpdates, UpdateIgnoreRule`；dependency-source 的 `isLocalDependency`；并 `export type { IgnoredUpdates, UpdateIgnoreRule }` 再转发
- `client/pages/dependencies/package-utils.ts:11` — `isBundlePackageName`, `shouldIncludeDiscoveredLocalPlugin`（原 dependencies.vue:130-131）
- `client/pages/dependencies/use-dependency-names.ts:13` — `isBundlePackageName`（原 dependencies.vue）
- `client/pages/dependencies/use-package-card-state.ts:10-11` — `isBundlePackageName`, `isLocalDependency`（原 package.vue:270-271）
- `client/dialogs/install/use-install-decision.ts:14` — `isBundlePackageName`；`use-install-versions.ts:14` — `isLocalDependency`（原 install/index.vue:143-144）
- `client/dialogs/bundle-install/bundle-format.ts:9-10` — `scanSensitiveConfig`（自 `src/shared/bundle-idents` 直引）；`use-bundle-members.ts:12-14` — `parseBundleManifest, PluginBundleManifest, getBundleGroupIdent`；`use-bundle-diff.ts:11` — `hasBundleKeyword, validateBundleManifest`（原 bundle-install/index.vue:338-344 一块）
- `client/shared/operations/bundle.ts` — `getBundleGroupIdent, getPluginShortname, isBundlePackageName, parseBundleManifest, PluginBundleManifest, PluginBundleRecord`（ident 函数自 `src/shared/bundle-idents` 直引）
- `client/dialogs/bundle-uninstall/index.vue:113-114` — `getBundleGroupIdent, PluginBundleRecord`
- `client/extensions/version/index.vue:77` — `PluginBundleRecord`
- `client/market/catalog.ts:10` — `hasBundleKeyword, isBundlePackageName`
- `client/market/state.ts:12` — `MarketProvider, MarketSnapshotResponse`；`client/market/snapshot-utils.ts:11-16` — `MarketLookupRequest, MarketSnapshotResponse, MarketSnapshotTransfer`；`client/market/lookup.ts:11-12` — `MarketLookupResult`, `collectServiceProviders`（均来自 `'../../src/shared'`，lookup.ts 为契约未提的新消费点）

## 2. Koishi 服务与数据通道

### 2.1 DataService 通道（client store 字段）

定义处格式：参考位置；**当前**位置。

| 通道名 | 定义处 | payload 类型 | authority | client 消费 |
|---|---|---|---|---|
| `market` | `MarketProvider`（参考 shared/index.ts:99 基类、src/node/market.ts:26 node 实现、src/browser/market.ts:4 browser 实现【已砍除】；当前 src/shared/provider.ts:27 基类、src/node/market/index.ts:26 实现） | `MarketProvider.Payload`（registry, data, dataVersion, **revision**, total, failed, progress, gravatar, stale, error, cached, cachedAt, validatedAt, serverNow, refreshing, loading, debug） | 4 | `store.market`（client/index.ts:41-59，页面 fields: ['market']） |
| `dependencies` | `DependencyProvider`（参考 src/node/deps.ts:7-15；当前 src/node/console/providers.ts:19-27） | `Dict<Dependency>`（参考 installer-types.ts:44：request/resolved/workspace/source/local/bound/invalid/latest；当前 src/core/deps/types.ts:15-38） | 4 | `store.dependencies` |
| `registry` | `RegistryProvider`（参考 deps.ts:17-25；当前 providers.ts:30-40） | `Dict<Dict<Pick<RemotePackage, DependencyMetaKey>>>` | 4 | `store.registry` |
| `registryStatus` | `RegistryStatusProvider`（参考 deps.ts:27-35；当前 providers.ts:43-51） | `Dict<RegistryStatus>` | 4 | `store.registryStatus`（sweep 逻辑当前在 client/app/registry-state.ts:44-102） |
| `marketData` | `MarketDataStore`（参考 src/node/data.ts:38-49；当前 src/node/market/data-store.ts:92），`immediate: true` | `MarketDataStorePayload`（override/updateIgnored/bundleRecords/collapsedGroups） | 4 | `store.marketData`（client/shared/plugin-config/data-store.ts:35） |

### 2.2 RPC / 事件监听器全集（`ctx.console.addListener`，全部 `{ authority: 4 }`）

注册位置：参考 `参考/src/node/console-listeners.ts:19-147`（拆分前在 src/node/index.ts:1573-1707）；**当前**拆为 `src/node/console/listeners/{install,upload,market}.ts`（入参 zod 契约 `src/node/console/contracts.ts`，24 个 schema），另有 `market/refresh` 在参考 shared/index.ts:107 / 当前 src/shared/provider.ts:31。

| 事件名 | 参数 | 返回 | 实现 | 参考注册行 |
|---|---|---|---|---|
| `market/install` | `deps: Dict<string>`, `forced?`, `options?: InstallOptions` | `Promise<number>` | `installer.install` + ensurePluginConfigs + refresh 4 通道 | 19 |
| `market/install-bundle` | `request: BundleInstallRequest`, `forced?`, `options?` | `Promise<BundleInstallResult>` | `installBundle()`（本地函数） | 40 |
| `market/install-fallback-candidate` | `failedEndpoint?: string` | `Promise<InstallFallbackCandidate \| undefined>` | `installer.getInstallFallbackCandidate` | 45 |
| `market/install-history` | `limit?: number` | `Promise<InstallHistoryEntry[]>` | `installer.getInstallHistory` | 49 |
| `market/install-history-detail` | `id: string` | `Promise<InstallLogDetail \| undefined>` | `installer.getInstallLogDetail` | 53 |
| `market/local-package-upload-start` | `request: LocalPackageUploadStartRequest` | `Promise<LocalPackageUploadStartResult>` | `installer.startLocalPackageUpload` | 57 |
| `market/local-package-upload-chunk` | `request: LocalPackageUploadChunkRequest` | `Promise<LocalPackageUploadProgress>` | `installer.appendLocalPackageUpload` | 61 |
| `market/local-package-upload-finish` | `request: LocalPackageUploadFinishRequest` | `Promise<LocalPackageUploadPreview>` | `installer.finishLocalPackageUpload` | 65 |
| `market/local-package-upload-commit` | `uploadId: string` | `Promise<LocalPackageUploadCommitResult>` | `installer.commitLocalPackageUpload` | 69 |
| `market/local-package-upload-cancel` | `uploadId: string` | `Promise<boolean>` | `installer.cancelLocalPackageUpload` | 73 |
| `market/prepare-local-binding` | `name: string` | `Promise<LocalBindingResult>` | `installer.prepareLocalBinding` | 77 |
| `market/environment-snapshots` | — | `Promise<EnvironmentSnapshotSummary[]>` | `installer.getEnvironmentSnapshots` | 81 |
| `market/environment-snapshot-preview` | `id: string` | `Promise<EnvironmentSnapshotPreview \| undefined>` | `installer.getEnvironmentSnapshotPreview` | 85 |
| `market/environment-snapshot-apply` | `id, options?: InstallOptions` | `Promise<number>` | `installer.applyEnvironmentSnapshot` + refresh | 89 |
| `market/remove-bundle-configs` | `request: BundleConfigRemoveRequest` | `Promise<BundleConfigRemoveResult>` | 本地 `removeBundleConfigs()` | 99 |
| `market/update-config` | `patch: Partial<Config>`（白名单键 configPatchKeys，参考 config.ts:128） | `Promise<boolean>` | 本地 `updateMarketNextConfig()`（写 loader 配置） | 103 |
| `market/update-data` | `patch: Partial<MarketDataStorePayload>` | `Promise<MarketDataStorePayload>` | `dataStore.patch` | 107 |
| `market/refresh-dependencies` | — | `Promise<void>` | `installer.refresh(true)` + refresh('config') | 111 |
| `market/package` | `name: string` | `Promise<Registry \| undefined>`（类型精确化：缓存陈旧时 resolve undefined，运行时行为与旧版一致） | `installer.getRegistry` | 116 |
| `market/index` | `request?: MarketSnapshotRequest`（transport: `'inline' \| 'http-gzip'`） | `Promise<MarketSnapshotResponse>` | `services.market.getSnapshot()`；http-gzip 时经 `MarketSnapshotTransport` 返回 `{transport, url, payload, decodedSize, encodedSize}` | 120 |
| `market/lookup` | `request: MarketLookupRequest`（names/services，可整体省略，实现内 `= {}` 兜底） | `Promise<MarketLookupResult>` | 本地 `lookupMarket()`（names≤512, services≤128，每项 trim、长度 ≤214、Set 去重） | 126 |
| `market/registry` | `names: string[]` | `Promise<Dict<Dict<Pick<RemotePackage, DependencyMetaKey>>>>` | `installer.getPackage` 并发 pMap（`config.concurrency ?? 4`） | 130 |
| `market/ensure-config` | `name: string` | `Promise<boolean>` | 本地 `ensurePluginConfig()` | 143 |
| `market/avatar` | `key: string`, `url?: string` | `Promise<AvatarFetchResult \| undefined>`（`{data, type, cached?}`） | 本地 `fetchAvatar()`（内存+磁盘缓存、SSRF 防护，见附录 C） | 147 |
| `market/refresh` | — | `Promise<void>` | `MarketProvider.start(true)` | shared/index.ts:107 |

client 端调用点核对（全部为 `send(...)`，无 `service()` 调用；位置为当前布局）：`market/install`(shared/operations/install.ts)、`market/install-bundle`(bundle-install/use-bundle-install.ts)、`market/install-fallback-candidate`(shared/operations/progress.ts)、`market/install-history`(install-history/index.vue:156)、`market/install-history-detail`(:184)、`market/local-package-upload-*`(pages/dependencies/use-local-package-upload.ts:78,92,103,131,198)、`market/prepare-local-binding`(pages/dependencies/use-local-binding.ts)、`market/environment-snapshots`(environment-versions/index.vue:205)、`market/environment-snapshot-preview`(:232)、`market/environment-snapshot-apply`(shared/operations/install.ts)、`market/remove-bundle-configs`(confirm/index.vue:118, bundle-uninstall/index.vue:324)、`market/update-config`(client/shared/plugin-config/config.ts)、`market/update-data`(client/shared/plugin-config/data-store.ts)、`market/refresh-dependencies` / `market/refresh`(client/app/actions.ts:121)、`market/package`(bundle-install/use-bundle-members.ts, shared/operations/{state,bundle}.ts)、`market/index`(market/state.ts)、`market/lookup`(market/lookup.ts)、`market/registry`(install/use-install-versions.ts, bundle-install/use-bundle-members.ts)、`market/ensure-config`(shared/operations/ensure.ts)、`market/avatar`(market/avatar/fetch.ts)。外部通道：`send('manager/remove', path, id)`（extensions/config-remove/index.vue:63，依赖 @koishijs/plugin-manager）。

### 2.3 广播事件（`ctx.console.broadcast` → client `receive()`）

发出方格式：参考位置；**当前**位置。5 个事件的名称、payload、节流全部一致。

| 事件名 | payload | 发出方 | client 接收方 |
|---|---|---|---|
| `market/patch` | `Partial<MarketProvider.Payload>`（增量 data delta） | 参考 node/market.ts:80（throttle 500ms）；当前 src/node/market/index.ts:77-82 | market/state.ts:189（合并快照） |
| `market/registry` | `Dict<Dict<Pick<RemotePackage, DependencyMetaKey>>>`（增量） | 参考 registry-metadata.ts:35（throttle 500ms）；当前 src/node/installer/wire.ts:178-183 | client/app/registry-state.ts:78（合并进 store.registry） |
| `market/registry-status` | `Dict<RegistryStatus>`（增量） | 参考 registry-metadata.ts:39（throttle 200ms）；当前 src/node/installer/wire.ts:141-144 | client/app/registry-state.ts:86 |
| `market/registry-status/clear` | `{}` | 参考 registry-metadata.ts:243；当前 src/node/installer/registry-status.ts:53-57（已无 `as any` 强转） | client/app/registry-state.ts:100 |
| `market/install-log` | `{ type: 'stdout'\|'stderr', line: string }` | 参考 install-history.ts:156；当前 src/node/installer/wire.ts:213-214 | client/shared/operations/progress.ts:57 |

### 2.4 HTTP 路由

- `GET {console.uiPath}/market-next/snapshot/:id` — 参考 src/node/index.ts:184-201；当前 src/node/setup.ts `setupSnapshotRoute`。gzip 市场快照（gzip level 6；`id = sha256(json)`，路由 URL `/market-next/snapshot/{id}`；LRU 上限 6 份；WeakMap 按 (data 对象 + dataVersion) 记忆化复用），配合 `market/index` 的 `http-gzip` transport。响应头全集：`Content-Encoding: gzip`、`Cache-Control: public, max-age=31536000, immutable`、`ETag: "{id}"`、`X-Content-Type-Options: nosniff`；404 文案 `market snapshot not found`。

## 3. 命令与配置

### 3.1 ctx.command（均在参考 `参考/src/node/commands.ts`，authority 4；当前 src/node/console/commands.ts）

| 命令 | 别名 | 选项 | 行为 | 参考行号 |
|---|---|---|---|---|
| `plugin.install <name>` | `.i` | — | resolveName → 查重 → findVersion → install + ensurePluginConfigs，写重启消息（try/finally 保证清空 envData.message） | 20 |
| `plugin.uninstall <name>` | `.r` | — | resolveName → `install({[name]: null})` | 49 |
| `plugin.upgrade [name...]` | `.update`, `.up` | `-s,--koishi`(self)、`-f,--force` | refresh(true,true) → getDeps → getUpdateCandidates/getLatestAllowedUpdate（忽略策略）→ 逐个列出版本、prompt 确认（只接受 `Y`/`y`）→ install | 64 |
| `plugin.clear-avatar-cache` | — | — | 清空头像内存+磁盘缓存，返回统计 | 133 |

resolveName 规则：`@koishijs/plugin-*` 原样；含 `(^|/)koishi-plugin-` 的原样；`@scope/name` → `@scope/koishi-plugin-name`；裸名 → `[@koishijs/plugin-x, koishi-plugin-x]` 双候选。重启消息 `ctx.loader.envData.message = {...pick(session, ['sid','channelId','guildId','isDirect']), content}`，结束后置 null。

### 3.2 Config Schema 完整结构（参考 `参考/src/node/config.ts`，接口 @6、Schema @90；当前 src/node/config/index.ts）

```
frontendMode: 'performance'|'polished' (radio, default 'performance')
depsLayout: 'grid'|'list' (radio, default 'grid')
idleProbe: boolean (default true)
idleProbeDelay: number(time, default 5min)
idleProbeBootDelay: number(time, default 1min)
idleProbeInterval: number(time, default 6h)
bulkMode: boolean (hidden, default false)
removeConfig: undefined|true|false (hidden)          # 卸载时是否删插件配置
updateIgnoredPackages: string(textarea, hidden)
updateIgnoreDuration: number(time, hidden, default 0)
updateIgnoreVersions: number(1-20, hidden, default 1)
updateIgnorePrerelease: boolean(hidden, default false)
collapsedGroups: Dict<boolean>(hidden)
registry: Installer.Config
search: MarketProvider.Config
chatlunaTool: boolean (default false)                # 【已随 ChatLuna 工具砍除】
marketSilentFilters: string(textarea, hidden)        # legacy
marketSilentStatusRules / marketSilentDateRules / marketSilentRecentRules / marketSilentCustomRules: Schema.any[] (hidden)
marketSilentRules: Array<{ type, value, note, enabled }> (table, default [])
```
`marketSilentRules.type` 枚举：`preview / insecure / bundle / created-before / created-after / updated-before / updated-after / created-within / updated-within / custom`（与当前代码一致）。

子配置：
- `Installer.Config`（参考 src/node/installer-types.ts）：`endpoint`(link)、`timeout`(time, default 5s)、`autoRoute`(default true)、`retry`(0-5, default 1)、`concurrency`(1-16, default 4)、`installLogRetentionHours`(default 72)；deprecated `installLogRetention`。
- `MarketProvider.Config`（参考 node/market.ts:706-709）：`endpoint`(link, default `'https://registry.koishi.t4wefan.pub/index.json'`，即 `DEFAULT_ENDPOINT`)、`timeout`(default 30s)、`proxyAgent`、`autoRoute`(default true)、`logLevel`(`silent|error|warn|info|debug`, default 'warn')。

配置由 `market/update-config` RPC 写回（白名单 `configPatchKeys`，参考 config.ts:128）；`idleProbe*` 变更触发插件 reload（`configReloadKeys`，参考 config.ts:149；当前 src/node/config/index.ts:183）。

## 4. src/browser 入口（浏览器打包）【重构已砍除，保留考据】

重构决策：砍除 browser 独立入口——tsdown 只构建 node/shared 双入口，addEntry 职责并入 `src/node/index.ts`（dev: `client/index.ts`，prod: `resolveConsoleClientRoot(...)`）。以下为旧实现考据。

`src/browser/index.ts`（30 行）：
- 导出：`export * from './market'`（browser/market.ts 无命名导出）、`export * from '../shared'`、`export { MarketProvider }`（browser 实现）、`filter = false`、`name = 'market'`、`inject = ['console']`、空 `Config`、`apply(ctx)`。
- `apply` 做三件事：(1) `ctx.plugin(MarketProvider)`（browser 版，DataService `'market'`）；(2) 注册 `market/index` 监听器（无参调用 `services.market.getSnapshot()`，不支持 http-gzip）；(3) `ctx.console.addEntry(...)` 指向 `client/index.ts`（dev）或 `KOISHI_BASE/dist`（静态环境，用 `KOISHI_BASE` 环境变量回退到 `/dist/index.js`，否则 `import.meta.url` 替换成 `/client/index.ts`）。

`src/browser/market.ts`（23 行）：`MarketProvider extends BaseMarketProvider`，`collect()` 直接返回 `this.ctx.loader.market`（Koishi WebUI 内置 registry 数据），`get()/getSnapshot()` 把 objects 映射为 `{data: {name: SearchObject}, failed:0, total, progress}`。用途：在浏览器/纯前端构建（无 node HTTP）里提供同一套 market 通道与类型。若未来恢复 browser 场景，需同步恢复 package.json browser 条件导出、tsdown 入口与基类的 `collect()/prepare()` 钩子。

## 5. Installer 服务 public 方法清单（`参考/src/node/installer.ts`）

参考类声明 @88（拆分后 803 行）；下表行号对应**拆分前**的大文件版本。install/override 族拆至 `installer-transaction.ts`，local-upload 族拆至 `local-upload.ts`，environment 族拆至 `environment.ts`，getInstallHistory/getInstallLogDetail 拆至 `install-history.ts`。当前实现：`src/node/installer/index.ts` + `src/core/install/**`。

public 字段：`http`、`endpoint`、`fullCache`、`tempCache`、`registryStatus`、`config`、`ctx`；getter：`cwd`、`isInstalling`。

| 方法 | 签名要点 | 用途一句话 | 旧行号 |
|---|---|---|---|
| `start()` | — | 服务启动：载入路由统计、清理日志、重置 endpoint、记录启动环境快照、调度元数据探测 | 321 |
| `resolveName(name)` | → `string[]` | 短名 → 候选包名列表（规则见 §3.1） | 415（拆分后 167） |
| `findVersion(names)` | → `Promise<Dict<string>>` | 在候选名中找最新可用版本 | 426 |
| `getInstallFallbackCandidate(failedEndpoint?)` | → `InstallFallbackCandidate \| undefined` | 按路由评分推荐备用 npm 源（label/reason） | 688 |
| `getRegistry(name, serial?)` | → `Promise<Registry \| undefined>`（缓存陈旧返回 undefined） | 带重试/竞速/状态上报的 npm 元数据获取 | 770 |
| `setPackage(name, versions)` | — | 外部注入包版本缓存并广播 | 908 |
| `getPackage(name)` | → `Promise<Dict<Pick<RemotePackage,DependencyMetaKey>> \| undefined>` | 带任务去重与 404 缓存的版本获取 | 914 |
| `refreshDependencyMetadata(wait)` | → `Promise \| undefined` | 后台刷新依赖 latest（幂等） | 1030 |
| `probeDependenciesInBackground(reason)` | — | 空闲探测：重置缓存并完整刷新依赖与市场数据 | 1045 |
| `getDeps(options?)` | → `Dict<Dependency>` / Promise | 依赖快照（可选触发元数据刷新） | 1071（拆分后 342） |
| `refreshData()` | — | 刷新 dependencies/registry/registryStatus/packages 四通道 | 1080 |
| `refresh(refresh?, waitMetadata?)` | — | 全量重置依赖缓存与元数据 | 1089 |
| `getInstallHistory(limit=20)` | → `InstallHistoryEntry[]`（clamp 1-50） | 安装日志历史列表 | 1354（拆分后 391） |
| `getInstallLogDetail(id)` | → `InstallLogDetail \| undefined` | 单条安装日志内容（head/tail 截断，见附录 E） | 1377 |
| `getEnvironmentSnapshots()` | → `EnvironmentSnapshotSummary[]` | 环境快照列表（标记 current） | 1391 |
| `getEnvironmentSnapshotPreview(id)` | → `EnvironmentSnapshotPreview \| undefined` | 快照与当前环境 diff 预览 | 1399 |
| `exec(args)` | → `Promise<number>` | 运行包管理器（yarn berry JSON 解析、stdout/stderr 逐行广播+落盘；见附录 G） | 1414 |
| `override(deps)` | — | 直接写 package.json dependencies（null=删除，按 localeCompare 排序、2 空格缩进+尾换行） | 1504 |
| `install(deps, forced?, beforeReload?, options?)` | → `Promise<number>` | 排队执行安装（快照/回滚/本地插件保护/fullReload 判定/日志；见附录 E） | 1807 |
| `startLocalPackageUpload(request)` | → `StartResult` | 开启本地 .tgz 分片上传会话（见附录 D） | 1811 |
| `appendLocalPackageUpload(request)` | → `Progress` | 追加 base64 分片（严格顺序/大小校验） | 1815 |
| `finishLocalPackageUpload(request)` | → `Preview` | 完成上传并解包校验，产出预览（operation 等） | 1819 |
| `commitLocalPackageUpload(uploadId)` | → `CommitResult` | 把归档原子移入 `.yarn/local` 并返回 file: 请求串 | 1841 |
| `cancelLocalPackageUpload(uploadId)` | → `boolean` | 取消并清理会话 | 1845 |
| `prepareLocalBinding(name)` | → `LocalBindingResult` | 对 unbound 本地插件执行 `npm pack` 并落地归档 | 1849 |
| `applyEnvironmentSnapshot(id, options?)` | → `Promise<number>` | 按快照恢复依赖环境（拒绝 unsupported 本地依赖，见附录 F） | 1948 |
| `isSelfUpdate(deps)` | → `boolean` | deps 是否包含本插件包名 | 1969 |

命名空间导出：`Installer.GetDepsOptions`（1975）、`Installer.Config` 接口与 Schema（1980-1998，拆分后在 installer-types.ts）。文件级导出：`InstallOptions`(124)、`InstallFallbackCandidate`(128)、`InstallHistoryStatus`(134)、`InstallHistoryChange`(136)、`InstallHistoryEntry`(144)、`LocalBindingResult`(157)、`InstallLogDetail`(163)、`Dependency`(180)、`YarnLog`(205)、`LocalPackage`(219)、`loadManifest(name, baseDir?)`(224)、`export default Installer`(2095)。

## 其他重构时需注意的隐式契约

- ChatLuna 工具【重构已砍除，保留考据】：工具名 `koishi_plugin_market_search`（`参考/src/node/chatluna-search.ts:5`；`applyChatLunaTool` 在 chatluna.ts:63；`ChatLunaToolConfig` @43）；依赖 `chatluna.platform.registerTool(name, {selector:()=>true, meta:{source:'extension',group:'market',tags,defaultAvailability}})`；索引缓存 TTL 10 分钟（模块级 cache/pending 按 endpoint，失败回退 stale 缓存）；zod searchSchema 参数面：intent 6 枚举（search/recommend/recent/popular/risk/compare）、status 5 枚举（verified/insecure/preview/portable/deprecated）、sort 4 枚举、limit 1-50 默认 10、日期四边界+两 withinDays、includeHidden/includeDeprecated；输出 SearchPayload JSON 结构（summary/nextQueries 等 LLM 友好字段）。若未来恢复：需移植 chatluna.ts + chatluna-search.ts 并在 koishi.service.optional 加回 `chatluna`、Config 加回 `chatlunaTool`。
- 磁盘路径契约：`data/market-next.json`、`data/market-next-environment-snapshots.json`、`data/market-next-install-logs/`、`cache/market-next-avatars/`、`cache/market-next-registry-stats.json`、`cache/market-next-index.json`、`cache/market-next-index/`、`.yarn/local/`（及 `file:.yarn/local/xxx.tgz` 请求串格式）——P6 已逐项核对与当前代码一致（含临时目录 `.market-next-upload` / `.market-next-pack-` 前缀）。
- 旧包名硬编码点（数据/配置兼容关键，P6 补全考据）：参考代码 `SELF_PACKAGE = 'koishi-plugin-market-next'` 硬编码于 `installer-types.ts:10`、`console-listeners.ts:11`、`plugin-config.ts:15`、`config.ts:165`、`index.ts:148`、`client/utils.ts:404`、`client/components/utils.ts:249`（`MARKET_NEXT_PACKAGE`）、`client/components/environment-versions.vue:219`（自身更新检测 `change.name === 'koishi-plugin-market-next'`）；配置节点匹配接受插件名 `market-next` 或 `koishi-plugin-market-next`（`~` 禁用前缀、`group:` 递归、禁用节点作 fallback）。重构版包名为 `koishi-plugin-marketn-refactored`（self-import 与 self-update 检测同步换名），存量用户升级时 data/market-next.json 文件名与磁盘路径**不变**（沿用旧名）。
- client 注册的 Console 扩展点：页面 `market`(/market, fields:['market'])、`dependencies`(/dependencies, fields:['dependencies','registry','registryStatus'])；slot 类型 `welcome-choice`、`global`(×7)、`status-right`、`plugin-dependency`、`plugin-details`、`plugin-missing`、`plugin-select`；action `market.refresh`(ctrl+r)、`market.install`、`dependencies.manual/history/versions`；menu `market`、`dependencies`；并猴补 `config.tree.remove` action 与 label（extensions/index.ts）。
- `client/market/index.ts` 是可复用组件库出口：`MarketIcon, MarketFilter, MarketList, MarketSearch` + `export * from './utils'`（注意：不含 `MarketPackage`；旧 utils.ts 约 30 个导出已拆至 catalog/filter/recommend/similarity/sort/users/lookup/avatar/ 等子模块）。
- client 页面消费 `store.packages`、`store.registry`、`store.market`、`store.dependencies`、`store.marketData`、`store.registryStatus`；`receive` 处理器共 5 个（见 2.3 表）。
- node/index.ts `apply` 中的插件编排顺序：`applyChatLunaTool`【已砍除】 → `ctx.plugin(Installer, config.registry)` → inject(installer) 注册命令 → inject(console,installer,server) 注册 3 个 Provider + MarketDataStore + `ctx.plugin(MarketProvider, config.search)` + addEntry + 快照路由 + listener + ready 钩子（dataStore.migrateFromConfig、插件配置修复、头像缓存清扫）。

---

# 策略与算法考据（P6 补全——删除 `参考/` 前的最后抢救）

> 以下为 `参考/src/node` 中未写进上文各节、但删除后无法从产物反推的实现知识。数值均来自参考代码，是"重构前后行为等价"验收与未来调参的唯一依据。

## 附录 A. npm registry 路由与 failover（registry-router.ts / route-health.ts / registry-metadata.ts）

**端点与竞速**：`REGISTRY_FALLBACK_ENDPOINTS` 五镜像按序：`registry.npmmirror.com` → `mirrors.cloud.tencent.com/npm` → `mirrors.huaweicloud.com/repository/npm` → `registry.npmjs.org` → `r.cnpmjs.org`；`REGISTRY_ROUTE_STAGGER = 120ms`（备用端点错峰启动）；`REGISTRY_FAST_ROUTE_THRESHOLD = 800ms`（主端点慢速阈值/竞速触发线）；`REGISTRY_STATS_TTL = 30 天`（路由统计文件过期即丢弃）。endpoint 未配置时用 `get-registry` 包读用户 .npmrc。竞速机制：主端点先发，超 800ms 或失败则全部备用端点按 `120ms × index` 错峰并发，先回先得，其余 abort。

**健康评分公式（`scoreRouteHealth`，两域共用）**：成功率项 `(rate-0.5)*6`；total≥3 且 rate≥0.8 加 1.5、rate<0.35 减 2；历史 score 直加；成功次数 `+min(2, successes*0.25)`、失败 `-min(2, failures*0.2)`；平均延迟分档 ≤300ms +1.5 / ≤fastThreshold +1 / ≤1200ms +0.5 / ≤2500ms −0.3 / ≤4000ms −1 / 更慢 −2；compressionBonus 时 br +0.5、gzip +0.2；10 分钟内成功过 +1.5；连续失败 `-min(5, consecutive*1.5)`。

**记分与冷却**：成功记分 ≤fast 阈值 +0.4 否则 +0.1，failures 衰减 ×0.6，EWMA 均值 `0.7旧+0.3新`；失败罚分按原因：not-found 0.4 / invalid 0.8 / http 1.2 / timeout、network 1.8 / 其他 1.5（market 域固定 1.2）；registry 磁盘落盘 debounce 2s，恢复时 1 天内有成功则 score clamp(-1,3) + failures 截断 `min(failures, max(2, ceil(successes/2)))`，否则 clamp(-4,3) + failures≤12；主端点 demote 条件 `failures≥2 且 selectedScore+1 < primaryScore`；fallbackDelay 自适应：无统计 800ms / 10min 无成功且失败≥3 → 200ms、≥2 → 400ms / avg>4000ms → 400ms / >2500ms → 600ms；端点变更时 routeStats 全部重置。market 域冷却阶梯：1 fail=1min、2=5min、3=30min、4=4h、5+=12h。

**registry 元数据获取（registry-metadata.ts）**：404 缓存 TTL 5 分钟；重试循环 `retry+1` 次，退避 `sleep(300×(retry+1))` 线性；失败原因聚合：全 not-found 才报 not-found，否则取第一个非 not-found（`allRegistryAttemptsNotFound`/`getRegistryAttemptReasons`，错误对象挂 `marketNextReason`/`marketNextReasons` 两个 defineProperty 属性）；版本过滤：`koishi` 包特判 `satisfies(version,'4')`，其他插件 `Scanner.isPlugin && Scanner.isCompatible('4', remote)`；`getVersions` 只保留 peerDependencies/peerDependenciesMeta/deprecated 三键并按 semver 降序；`pickMetadataProbe` 优先 `koishi` → `@koishijs/plugin-console` → 任一插件 → 第一个。

## 附录 B. 市场索引：镜像、版本共识与磁盘缓存（market-internals.ts / market-version.ts / market-router.ts / market-cache.ts / market.ts）

**镜像清单（`FALLBACK_ENDPOINTS`，10 个）**：t4wefan（默认）、gitee 聚合器、`kp.itzdrli.cc`、`koishi.itzdrli.cc`、`registry.koishi.chat`、koishijs.github.io、raw.githubusercontent(release)、cdn.jsdelivr.net、ghproxy.net 代理、ghfast.top 代理。`ROUTE_STAGGER = 80ms`、`FAST_ROUTE_THRESHOLD = 500ms`（注意与 registry 域的 120/800ms 不同）、`FIRST_PAYLOAD_TIMEOUT = 1.5s`、`MARKET_GENERATION_TOLERANCE = 2 分钟`、`MAX_CACHE_ENTRIES = 3`、`CACHE_ENTRY_TTL = 30 天`。usage 镜像文档比 FALLBACK 多一个 `https://koi.nyan.zone/registry/index.json`（Lipraty）与 k.ilharp.cc 论坛链接（见 §1.2）。

**版本见证（共识）机制**：7 个 versioned source 带权威权重：`registry.koishi.chat`(100, authoritative)、`raw.githubusercontent.../release`(95, authoritative)、`koishijs.github.io`(90)、`registry.koishi.t4wefan.pub`(85)、`cdn.jsdelivr.net`(70)、`ghfast.top`(65)、`ghproxy.net`(60)。`version.txt` 放在 `index.json` 同路径（正则替换），内容寻址 URL 为 `index.{64位hash}.json`，hash 须匹配 `/^[a-f0-9]{64}$/`。算法：前 3 个 primary 见证并发；`PRIMARY_DISCOVERY_TIMEOUT = 1.2s` 时若票数一致即定，否则开 secondary；总超时 `VERSION_DISCOVERY_TIMEOUT = 3s`；authoritative 见证返回或票面 authority 总和 ≥160 时立即定稿；选 hash 排序：票数 > 含 authoritative > 最大 authority。preferred 顺序：endpoints[0]、koishi.chat、t4wefan、raw.githubusercontent、github.io。

**获取与降级**：版本化获取失败降级直连；无缓存时投机预取（hardcode t4wefan 为第二候选，等待 300ms（无 resolution）/150ms（有 resolution）后 hash 匹配则采用，不匹配丢弃）；世代（generation）防回退：`minimumGeneration` 取所有缓存端点的 `forceTime||time` 最大值，容忍 2 分钟，低于此值拒绝并转入 rescue（冷却中的端点在活跃端点全失败后作为救援重试，rescue 失败仅罚 0.25 分且不延长冷却）。【重构砍除项：世代防回退与 `primary-stale` 归因未移植，fallbackReason 收缩为 3 值。】

**磁盘缓存 v3 双层结构**：主文件 `cache/market-next-index.json`（store v3：entries + lastUsed + routeStats）+ 分片目录 `cache/market-next-index/`（文件名 = `sha1(endpoint).hex.slice(0,16).json`，只存 result）；原子写 `.{pid}.{ts}.tmp` + rename；挑选评分：age≤12h +3 / ≤3d +1 / 其余 −1 / 是 config.endpoint 再 +0.5，lastUsed 最优先、config.endpoint 次之；条目上限 3、TTL 30 天，写时清理孤儿分片；legacy 内联 store（entries 内嵌 objects 数组）检测后 1s 延迟迁移为分片布局；条件请求头 `if-none-match`/`if-modified-since`（仅非内容寻址请求）；请求头 `accept-encoding: br,gzip,deflate`；响应 hash 为 sha256(解码后文本)。

**getSnapshot 首屏等待策略（market.ts）**：冷启动优先等磁盘预热任务（上限 1.5s）；首次 get 自行触发 warm 且只等 `min(400ms, 1.5s)`；网络仍无果则返回 `loading:true` 的空 payload 并在后台完成后 `refresh('market')`；有缓存而网络失败时返回 `stale:true` + error 的旧 payload；`dataVersion` 仅在 contentHash 变化时递增（payload data 按 dataVersion 记忆化复用）。

## 附录 C. 头像代理细则（avatar.ts）

常量：TTL 7 天、清扫间隔 1h、内存/磁盘各上限 512 条、单图上限 96KB、HEAD 超时 1200ms、GET 超时 3000ms、手动重定向上限 3 次。SSRF 防护：允许主机白名单 7 个（www.npmjs.com / npmjs.com / s.gravatar.com / gravatar.com / www.gravatar.com / cravatar.cn / www.cravatar.cn），白名单外做 DNS `lookup(all)` 解析并逐条检查私网地址；IPv4 私网判定（0/10、127/8、169.254、172.16-31、192.168、≥224 及畸形段）；IPv6（::1、::、::ffff: 前缀、fe80::/10（`first & 0xffc0 === 0xfe80`）、fc/fd/ff 前缀）；blocked：localhost / localhost.localdomain；流式读取超 96KB 中断。默认头像识别：gravatar `d=`/`default=` 参数值 ∈ {default,mp,identicon,monsterid,wavatar,retro,robohash,blank} 视为默认头像不缓存（`d=404` 例外），gravatar 键且 mode≠404 同样跳过；响应头 `avatar-from: default|mp` 判定。缓存键规范化（非法字符→`-`、截 128、空则 sha1 兜底），文件名 `sha1(key).json`；Accept 头 `image/avif,image/webp,image/png,image/jpeg,image/gif,image/svg+xml;q=0.8,*/*;q=0.1`。

## 附录 D. 本地包上传与绑定安全限制（local-upload.ts / local-binding.ts）

分片 512KB、会话 TTL 15 分钟、归档条目上限 8192、解压总大小上限 256MB、package.json 上限 1MB、总大小上限 64MB（`MAX_LOCAL_BINDING_PACK_SIZE`）。tar 参数 `strict:true, maxReadSize/maxMetaEntrySize 1MB, maxDecompressionRatio:200`；条目路径必须以 `package/` 开头、拒绝绝对路径与 `..`、拒绝 SymbolicLink/Link/CharacterDevice/BlockDevice/FIFO、拒绝重复 package.json；base64 分片严格校验（长度 %4、字符集、roundtrip 相等）；uploadId 须匹配 `/^[0-9a-f-]{36}$/i`；canonical 文件名 `{slug-120字符}-{version}.tgz` 尾插 `-{hash12}`（hash 须 `/^[a-f0-9]{12,64}$/i`）；commit 幂等（同 hash 复用、异 hash 报"同名本地插件归档已存在，但文件内容不一致"）；临时目录 `.yarn/local/.market-next-upload`（`.part` 后缀）。本地绑定：`npm pack --ignore-scripts --json --pack-destination`，超时 `max(1min, config.timeout)`；mkdtemp 前缀 `.market-next-pack-`；`file:.yarn/local/{filename}` 请求串；并发绑定竞态靠"rename 失败后重验目标 hash"解决。约 20 条用户可见中文错误文案（"请使用 npm pack 生成的 .tgz"、"归档中的包名不是有效的 Koishi 插件名称"等）。

## 附录 E. 安装编排细节（installer.ts / installer-transaction.ts / install-history.ts / bundle-installer.ts）

**安装前本地插件保护**：包管理器执行前，对"已安装、registry 形态、来源未确认"的依赖逐一 getPackage 确认；全部路由 404 → `markRegistryNotFoundDependency`（改判 unbound local，去掉 latest、清 invalid）；结果不确定时抛中文错误"暂时无法确认以下已安装插件是否来自 npm：{names}。为避免包管理器误下载本地插件，本次操作已取消；请检查 npm 网络后重试。"；存在 unbound 本地依赖时抛"检测到来源未绑定的本地插件，继续安装会让包管理器尝试从 npm 下载它们：{names}。请先在"本地插件"分组中绑定来源或移除这些依赖。"。

**包管理器调度判定**：`requiresPackageManager`——forced 恒真；删除恒真；local↔非 local 切换真；已 resolved 且 `satisfies(resolved, request, {includePrerelease:true})` 则可跳过包管理器（只写 package.json）；`hasDependencyRuntimeChange`（fullReload 判定）——resolved 变化 / 本地依赖请求变化。

**已知 quirk**：`require.resolve(name) in require.cache` 可能抛错（上游 FIXME koishijs/webui#273）；fullReload 延时 `FULL_RELOAD_DELAY = 1s`；override 写 package.json 时依赖按 `localeCompare` 排序、2 空格缩进+尾换行。

**安装日志**：文件名 `{ISO时间戳(:.→-)}-{deps概要截80字符}.log`，元数据 `.log.json`（version 1，changes 的 afterResolved 成功后回填）；详情上限 512KB（head 128KB + tail 384KB，中间插 `... N bytes omitted ...`），无元数据时预览 8KB+32KB；ANSI 清洗两条正则（OSC `\u001B]...\u0007/\u001B\\` 与 CSI 序列）+ 孤立 `\r`；legacy 按日志内容正则推断 status；retention：`installLogRetentionHours`（≥1h）> 废弃的 `installLogRetention` > 默认 3 天；getHistory clamp(1,50)。

**marketData 迁移与 bundle 安装**：`migrateFromConfig` 仅当 store 为空才从插件配置迁 updateIgnored/bundleRecords/collapsedGroups；`collapsedGroupsVersion=1` 迁移会删除 legacy `installed` 键；dispose 时 flush；原子写。bundle 安装：直接环检测（bundle↔member 互引）；成员配置键 `~{shortname}:{bundleMemberIdent}`（一律禁用态写入，preset 时值为 member.config）；move 语义（组外已有配置移入组、键冲突回退 ident 键）；`record.installedByBundle` = 安装前不在 dependencies。

## 附录 F. 环境快照（environment.ts）

上限 60 条、按 lastSeenAt 降序；ID = `env-{sha256(canonical).slice(0,20)}`，canonical 只含 `[name, local?request:(resolved||request), source, local]`——**request 的 `^`/`~` 差异不改变 ID**（同环境重装不产生新快照）；Windows rename 遇 EEXIST/EPERM 先删再 rename；diff 状态机顺序 unchanged→unsupported(local)→added→removed→upgrade/downgrade（双 valid semver 才 compare）→changed；恢复时 unsupported 本地依赖直接拒绝（中文文案"目标环境包含无法自动恢复的本地依赖"）。

## 附录 G. 环境变量与杂项 quirk

- `GRAVATAR_MIRROR` 环境变量（market.ts）：gravatar 基址切换开关，client 据此切换 gravatar 基址。
- legacy scanner 模式（无市场索引、走 npm registry analyze）两个 npmmirror quirk：npmmirror 404 静默忽略；npmmirror 缺 `links` 字段时补 `links.npm = endpoint.replace('registry.','www.') + '/package/' + name`。另有 `object.ignored` 字段过滤（现代索引模式剔除被忽略条目）。
- logLevel 过滤按 `['silent','error','warn','info','debug']` 序数比较；debug 级镜像为 `[debug]` 前缀的 **info** 记录（Koishi 日志页隐藏 debug 的绕过）。
- 包管理器 exec（package-manager.ts）：`which-pm-runs` 检测；yarn≥2（coerce+gte）追加 `--json` 并逐行解析 YarnLog（仅 `{` 开头行尝试）；npm/pnpm 自动在参数前插入 `install`；信号终止或无退出码 → **−1**；yarn 级别映射 warning→debug、error→warn。
- 私有 API quirk：`ctx.scope[Symbol.for('koishi.loader.record')]` 递归找父上下文；`ctx.loader.writeConfig(true)` 的 true 参数；直接调用 `ctx.console.listeners['config/request-runtime'].callback.call(null, name)` 请求运行时创建配置。
- idle probe 调度（idle-probe.ts）：客户端计数取 `ctx.console.clients`（size 或 keys 长度）；失败后重试门 `min(5min, interval)`；`console/connection` 事件驱动即时重排；boot 门与 interval 门独立计算。
