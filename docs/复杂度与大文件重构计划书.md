# 复杂度与大文件重构计划书（B+C 特化）

> 状态：**已执行（2026-08-25 收尾）**。执行方式：GLM5.3 主对话定基线 + 8 个并行 sub-agent 按目录划界拆分，主对话统一门禁与登记。验收结果见 §5/§6。
> 依据：2026-08-25 全量实测（`yarn check` 全绿 + fallow health 87 A + 17 个大文件清单）＋源码通读。
> 开发纪律见 [DEVELOPMENT.md](DEVELOPMENT.md)；分层约束见 [ARCHITECTURE.md](ARCHITECTURE.md)；契约基线见 [reference/前后端调用契约.md](reference/前后端调用契约.md)。

## 0. 背景与现状（2026-08-25 实测）

- **门禁**：`yarn check` 全绿（biome / tsc×2 / eslint / check-size 通过；17 个文件 >250 行是警告非 fail）。
- **覆盖率**：语句 86.9%、函数 86.4%（114 个文件）——重构有测试兜底，放心动手。
- **fallow health**：87 A，149 项超阈值（**44 critical + 35 high + 70 moderate**）。
  - ⚠️ `.fallow/*.json` 缓存是 **2026-08-21** 的旧快照（client 侧 `use-card-view.ts` 等路径已不存在），开工第一步先重跑 `yarn fallow health --format json` 取新基线，以新报告为准。
- **大文件**：src 侧 17 个文件 >250 行（见 §2 清单），其中 0 个 ≥350（暂无 fail 风险，但都需拆分或登记理由）。

## 1. 任务目标（可量化验收）

1. **结构**：§2 清单 17 个文件中，至少 12 个拆分到 ≤250 行；其余登记「不拆理由」到本文件 §5。
2. **复杂度**：fallow critical 从 44 降到 ≤15；health 分数从 87 提到 ≥90。
3. **质量**：`yarn check` + `yarn build` + `yarn test` 三绿；覆盖率不下降（以 86.9% 语句为底线）。
4. **契约**：对外契约面**零改动**——DataService 通道 / RPC / 广播 / HTTP 路由 / 命令 / 导出面（`client/index.ts`、包名导入的全部类型）原样保留。
5. **认知**：把「真重构对象」与「数据驱动映射（豁免登记）」的甄别结论固化到本文件 §4/§5，避免后人重复纠结。

## 2. 大文件清单（src 侧，check-size 口径 = 总行数）

> 全部为警告级（>250），未达 fail（≥350）。目标区间每文件 ≤200 行。

| # | 文件 | 行数 | 拆分方向（初步，具体以调研为准） | 难度 |
|---|---|---|---|---|
| 1 | `src/shared/bundle.ts` | 322 | 类型（7 接口 + 常量）与校验函数族（parse/validate）分离成 `bundle-types.ts` + `bundle-validate.ts`；注意 `registry/manifest.ts` 有 CJS 静态访问收敛，别动它的豁免 | 中 |
| 2 | `src/core/upload/session.ts` | 309 | `LocalPackageUploadStore` 类 + 内部接口/工具函数拆分；纯格式化/校验函数可出仓 `session-utils.ts` | 中 |
| 3 | `src/core/market/source/index.ts` | 305 | `MarketIndexSource` 类的 `applyIndex`/`fetchAndApply`/`scheduleRefreshAfterPrepare` 等状态更新逻辑抽成纯函数模块（如 `apply.ts`）；`updateState` 已在 cache 域，别混淆 | 高 |
| 4 | `src/node/market/data-store.ts` | 302 | `MarketDataStore` 类 + `normalizeStore`/`normalizeDict`/`isRecord` 防御函数出仓 `normalize.ts`；`migrateFromConfig`（CC=15）拆出迁移子模块 | 中 |
| 5 | `src/node/market/bundle.ts` | 298 | `removeBundleConfigs` 与 `installBundle` 两大函数族拆成 `bundle-remove.ts` / `bundle-install.ts`，共享 helper 留 `bundle.ts` | 中 |
| 6 | `src/core/install/pipeline/install-executor.ts` | 278 | `InstallExecutor` 的私有流程方法（prepare/finalize/apply 三阶段）拆到独立文件；`detectFullReload`（CC=15）提炼 | 高 |
| 7 | `src/core/deps/resolver.ts` | 275 | `DependencyResolver` 类 + 依赖比较/规范化纯函数分离；`getLocalDepsSnapshot` 大方法可再分 | 高 |
| 8 | `src/core/install/logs/reader.ts` | 271 | legacy 解析系列（`parseLegacyInstallLog`/`parseLegacyFields`/`resolveLegacyStatus` 等，CC 17）整组出仓 `legacy.ts` | 中 |
| 9 | `src/node/installer/wire.ts` | 272 | `createInstallerCore` 组装逻辑与类型分离；logger 工厂可出仓 `logger.ts` | 低 |
| 10 | `src/core/environment/snapshot.ts` | 261 | 类型 + 纯函数（normalize/canonical/create/summarize）与 `EnvironmentSnapshotStore` 类分离 | 中 |
| 11 | `src/core/market/cache/index.ts` | 263 | `MarketDiskCache` 类的 `updateState`（CC=22）/`load`（CC=17）状态机逻辑抽成纯函数；`buildCacheMeta` 等已有 helper 归位 | 高 |
| 12 | `src/core/registry/client/fetch.ts` | 260 | `fetchRegistryWithRetry` 主流程与 report 系列（`reportLoadingStatus`/`reportFetchFailure`）分离 | 中 |
| 13 | `src/core/registry/client/index.ts` | 260 | `RegistryClient` 类 + 路由状态管理（recordRoute*）抽子模块 | 中 |
| 14 | `src/core/upload/local-binding.ts` | 281 | `prepareLocalBinding` 主流程与 `assertBindableDependency`（CC=7）/`parseNpmPackOutput` 分离 | 中 |
| 15 | `src/node/avatar/disk-cache.ts` | 289 | read/write/normalize/clear 四族拆 2-3 个文件；`normalizeAvatarDiskCache`（CC=15）归入 normalize 文件 | 中 |
| 16 | `src/node/installer/index.ts` | 259 | mixin 组装型文件，拆分价值低——**登记不拆**（mixin 模式天然聚合，拆散反而破坏结构） | — |
| 17 | `src/shared/types.ts` | 267 | 纯类型字典文件——**登记不拆**（类型集中是特性；check-size 对 .d.ts 不查，对 .ts 查但类型文件拆散无收益） | — |

**拆分通则**：
- 拆出的新文件每个 ≤200 行目标；宁可多拆几个小文件，不要拆出 240 行的"半大文件"。
- `src/core/**` 拆出的新文件同样受依赖方向门禁（禁 koishi 运行时 import），别把 `import type { Dict }` 之外的东西带过去。
- 契约面 re-export：若拆分影响 `src/shared/index.ts` / `src/core/…/index.ts` 的导出面，用 `export type { ... }` 原样转发（参考 `client/shared/plugin-config/index.ts` 的聚合出口模式）。
- 每个文件拆完立刻跑一次 `yarn check:size` 确认行数下降，别攒到最后。

## 3. 复杂度治理清单（fallow critical 44 项，甄别后分类）

> 新基线以开工后重跑的 fallow 为准；下表基于 2026-08-25 实测。核心判断：**真重构（拆函数/抽查表）** vs **数据驱动映射（登记豁免）** vs **模板复杂度（不处理）**。

### 3.1 真重构候选（拆函数 / 抽查表，有明确收益）

| 位置 | 复杂度 | 建议 |
|---|---|---|
| `client/market/filter.ts` `validate` | CC=46（74 行） | 按操作符拆 `validateIs`/`validateDate`/`validateWithin`/`validateMeta`/`validateText`，主函数变分发表；**纯函数，测试已有，风险低** |
| `client/dialogs/bundle-install/use-bundle-members.ts` `<arrow>` | CC=29 | 成员解析/冲突判定抽独立纯函数 |
| `client/pages/dependencies/use-package-card-status.ts` 族（detailText 23 / statusLabel 19 / targetText 18 / sourceText 18 / statusClass 17 / statusIcon 16 / versionSourceText 10） | 7 项 | 状态 → 文本/图标/样式是**查表场景**：定义状态机配置表（数组/对象字面量），循环查表代替 if 链；若查表改造收益不明确，**整体登记豁免**（数据驱动映射）二选一，别半吊子 |
| `src/node/avatar/disk-cache.ts` `normalizeAvatarDiskCache` | CC=15 | 防御性校验拆小函数 |
| `src/node/avatar/index.ts` `fetchAvatar` | CC=12（38 行） | 候选链解析（gravatar/cravatar/npm）拆独立模块 |
| `src/node/console/lookup.ts` `lookupMarket` | CC=13 | 查询词分派拆小函数 |
| `src/node/console/commands.ts` 两个 `<arrow>` | CC=11/10 | 命令 handler 各拆一函数 |
| `client/pages/dependencies/use-dependency-groups.ts` `visibleGroups` | CC=11 | 分组过滤条件拆命名谓词 |
| `client/pages/dependencies/use-dependency-names.ts` `names` | CC=13 | 名称渲染逻辑抽纯函数 |
| `client/pages/market/use-market-debug.ts` `debugItems` | CC=15 | 调试项生成拆小段 |
| `client/dialogs/install/use-install-flow.ts` `installDep` | CC=14（55 行） | 安装决策流程拆子步骤 |
| `src/node/market/data-store.ts` `migrateFromConfig` | CC=15 | 见 §2 #4 |
| `src/core/install/pipeline/install-executor.ts` `detectFullReload` | CC=15 | 见 §2 #6 |
| `src/core/install/logs/reader.ts` `parseLegacyInstallLog` | CC=17 | 见 §2 #8 |
| `src/core/install/logs/store.ts` `finish` | CC=13 | 收尾状态机拆小 |
| `client/app/actions.ts` `onAprilFoolsShortcut` | CC=13 | 节日彩蛋逻辑拆小（低优先，可豁免） |

### 3.2 豁免登记候选（不重构，登记理由到 §5）

| 位置 | 复杂度 | 豁免理由 |
|---|---|---|
| `client/dialogs/bundle-install/index.vue` `<template>` | CC=70 | **模板复杂度**：fallow 对 .vue template 的 CC 口径与 JS 分支不同，399 行模板是 dialog 本来就大；client 不在行数门禁内。除非顺手拆子组件，否则不动 |
| `client/dialogs/install/index.vue` / `market.vue` / `dependencies/package.vue` / `bundle-uninstall/index.vue` `<template>` | CC=37/36/29/18 | 同上，模板复杂度登记豁免 |
| `scripts/check-i18n.ts` `checkClientLocales` | CC=13 | 工具脚本非产物，无运行时风险 |
| `client/pages/dependencies/use-package-card-status.ts` 族 | 见 3.1 | 若选查表改造则做，否则豁免（二选一） |
| `client/dialogs/install/use-install-versions.ts` / `use-install-decision.ts` / `install-progress/index.vue` / `install-history/index.vue` / `confirm/index.vue` / `environment-versions/index.vue` 等 | CC≤13 | 状态文本/单选分支映射，逐项复核后登记豁免 |

### 3.3 高优先总序（建议执行顺序）

1. `client/market/filter.ts validate`（CC=46，纯函数+有测试，风险最低收益最大）→ 2. `src/core/market/cache/index.ts`（updateState/load）→ 3. `src/node/market/bundle.ts` 拆分 → 4. `src/core/install/logs/reader.ts` legacy 出仓 → 5. `src/node/market/data-store.ts` normalize+migrate → 6. `src/core/market/source/index.ts` → 7. `src/core/install/pipeline/install-executor.ts` → 8. 其余按 §2 难度低→高。

## 4. sub-agent 编排建议（GLM 主对话开）

> 目标：一个主对话吃满免费 sub-agent，并行做互不碰文件的模块。

```
主对话：定基线（重跑 fallow + yarn check）→ 派发 → 汇总（全量 check/build/test）→ 收尾登记 §5
├─ sub-agent A → src/shared/（bundle.ts #1 + types.ts #17 评估）
├─ sub-agent B → src/core/market/（source #3 + cache #11）
├─ sub-agent C → src/core/install/（executor #6 + logs #8 + logs/store）
├─ sub-agent D → src/core/upload/（session #2 + local-binding #14）+ src/core/environment（#10）
├─ sub-agent E → src/core/registry/（fetch #12 + index #13）+ src/core/deps（#7）
├─ sub-agent F → src/node/（market/bundle #5 + data-store #4 + avatar #15 + console）
└─ sub-agent G → client 复杂度（filter.ts validate + use-bundle-members + use-package-card-status 查表改造）
```

- 每个 sub-agent **只读自己的模块 + 只改自己的文件/__tests__**，互不碰文件（按目录划界，天然无冲突）。
- 主对话每收一个 sub-agent 结果就跑一次 `yarn check`（必要时 `yarn build:client` 验 .vue import），全部完成后跑全量三绿。
- sub-agent 需要调研时让它自己开 sub-sub-agent 或并行读文件，主对话只做整合与门禁。

## 5. 收尾登记（2026-08-25 执行完毕，主对话填写）

### 5.1 大文件处置表（17/17 全部处置：15 个拆分 + 2 个登记不拆）

| 文件 | 处置（拆分后行数 / 不拆理由） | 门禁状态 |
|---|---|---|
| `src/shared/bundle.ts` | 322 → **17**（聚合转发出口）；类型+常量 → `bundle-types.ts`(156)、校验函数族 → `bundle-validate.ts`(184)；18 个导出符号与拆前逐一比对一致 | 通过 |
| `src/core/upload/session.ts` | 309 → **236**；纯校验/格式化出仓 `session-utils.ts`(109)，上传生命周期方法留类 | 通过 |
| `src/core/market/source/index.ts` | 306 → **221**；`apply.ts`(103) + `refresh.ts`(85) 出仓，`host.ts` 80→114 归位性能域；契约成员全部保留为薄转发 | 通过 |
| `src/node/market/data-store.ts` | 303 → **249**；`normalize.ts`(32) + `migrate.ts`(80) 出仓，`migrateFromConfig`（CC15）收敛为 `buildMigrationPatch` 纯函数 | 通过 |
| `src/node/market/bundle.ts` | 299 → **19**（聚合转发）；`bundle-remove.ts`(93) + `bundle-install.ts`(210) | 通过 |
| `src/core/install/pipeline/install-executor.ts` | 278 → **124**；三阶段实现出仓 `install-phases.ts`(183)；`detectFullReload`（CC15）提炼 `isReloadRequired` 后约降为 3 | 通过 |
| `src/core/deps/resolver.ts` | 276 → **195**；`snapshot.ts`(96) + `metadata-refresh.ts`(56) 出仓；`getLocalDepsSnapshot` 循环体拆为 `resolveLocalDependency`，类本体留守保住 `.fallowrc` 豁免 | 通过 |
| `src/core/install/logs/reader.ts` | 271 → **212**；legacy 解析系列（CC17）整组出仓 `legacy.ts`(72) | 通过 |
| `src/node/installer/wire.ts` | 273 → **214**；`logger.ts`(23) + `types.ts`(83) 出仓 | 通过 |
| `src/core/environment/snapshot.ts` | 261 → **132**；类型+纯函数族出仓 `snapshot-model.ts`(148)，`EnvironmentSnapshotStore` 留守并原样转发 | 通过 |
| `src/core/market/cache/index.ts` | 263 → **195**；`state.ts`(52) + `select.ts`(91) 出仓（评分/淘汰排序链全部直接测试，select 5.88%→94%、state 21%→100%）；`buildCacheMeta`（CC15）拆 `pickConditional`/`pickStat` | 通过 |
| `src/core/registry/client/fetch.ts` | 260 → **198**；report 系列出仓 `fetch-report.ts`(76) | 通过 |
| `src/core/registry/client/index.ts` | 260 → **13**（聚合转发）；`registry-client.ts`(209) + `route-state.ts`(90) + `deps.ts`(36)，`endpoints.ts` 142→169 | 通过 |
| `src/core/upload/local-binding.ts` | 281 → **204**；`local-binding-utils.ts`(105) 出仓（含 `assertBindableDependency`/`parseNpmPackOutput`） | 通过 |
| `src/node/avatar/disk-cache.ts` | 290 → **106**；`memory-cache.ts`(68) + `disk-entry.ts`(105) + `disk-cleanup.ts`(127)；`normalizeAvatarDiskCache`（CC15）拆 5 个校验小函数后约降为 6 | 通过 |
| `src/node/installer/index.ts` | **登记不拆**：mixin 组装型门面（一行转发+生命周期，各自转发到不同 core 类），拆散必须把 private 字段升格 `public declare`，扩大耦合面、破坏 mixin 聚合 | 通过（260 行警告已登记） |
| `src/shared/types.ts` | **登记不拆**：纯类型协议字典（20 接口 + 1 别名，node/client 间线上传输结构），类型集中是特性，拆散需动 `index.ts` 契约面且无收益 | 通过（268 行警告已登记） |

### 5.2 复杂度真重构成果（fallow 口径）

| 函数 | 前 → 后 | 方式 |
|---|---|---|
| `client/market/filter.ts` `validate` | CC 46 → 8 | 按操作符拆分派：`dateFilterRules` 前缀表 + `validateDateFilter`/`validateStatusWord`/`resolveStatusFlag`/`validateManifestWord` |
| `client/dialogs/bundle-install/use-bundle-members.ts` watch | CC 29 → 6 | 拆 6 个模块级纯函数（manifest 解析/冲突判定/成员状态） |
| `client/dialogs/install/use-install-flow.ts` `installDep` | CC 14 → 4 | 拆 `applyBulkOverride`/`resolveRemoveConfig`/`persistRemoveChoice`/`applyInstallResult` |
| `client/pages/dependencies/use-dependency-names.ts` `names` | CC 13 → 6 | 拆 `resolveDiscoveredInput`/`shouldIncludeLocalPackage` |
| `client/pages/dependencies/use-dependency-groups.ts` `visibleGroups` | CC 11 → 5 | 拆 `matchesFilter` 谓词 |
| `src/core/install/pipeline/install-reload.ts` `detectFullReload` | CC 15 → ~3 | 提炼 `isReloadRequired` 单依赖判定 |
| `src/core/install/logs/store.ts` `finish` | CC 13 → 2 | 拆 `writeFinishMarker`/`finalizeMetadata`/`resetSession` |
| `src/node/market/data-store.ts` `migrateFromConfig` | CC 15 → ~3 | 出仓 `buildMigrationPatch` 纯函数 |
| `src/node/avatar/disk-cache.ts` `normalizeAvatarDiskCache` | CC 15 → 6 | 拆 5 个防御性校验小函数 |
| `src/node/avatar/index.ts` `fetchAvatar` | CC 12 → 6 | 网络管线收拢 `fetchAvatarFromNetwork`（fetch.ts/body.ts 出仓） |
| `src/node/console/lookup.ts` `lookupMarket` | CC 13 → ~3 | 拆归一化/空结果/快照填充三函数 |
| `src/node/console/commands.ts` 两处 arrow | CC 11/10 → ~6/5 | 提炼 `resolveUpgradeTarget`/`resolveRequestedNames` 等具名函数 |
| `src/core/market/cache/persistence.ts` `buildCacheMeta` | CC 15 → 主体归零 | 拆 `pickConditional`/`pickStat` 字段读取 |

### 5.3 豁免登记表

**代码内 `// fallow-ignore-next-line complexity`**（理由随注释写在源码中）：
`use-package-card-status.ts` 六函数（状态→文本/图标/样式映射，判定顺序互不相同，统一状态机会改变可观察输出）、`use-package-card-meta.ts` `sourceText`（来源优先级映射）、`dependency-helpers.ts` `classify`、`package-utils.ts` `resolveIdentity`、`use-market-debug.ts` `debugItems`、`use-market-page.ts` `marketLoading`、`use-package-visibility.ts` `canExpandCard`、`use-dependency-*` 相邻小函数、`actions.ts` `onAprilFoolsShortcut`（彩蛋）、`koishi-eye-splash.vue` `updateEnhancements`（开场动画）、`use-install-versions.ts` 两个 watch、`use-install-decision.ts` `getResultText`、`use-local-package-upload.ts` `uploadFile`、`use-bundle-install.ts` `runInstall`、`market.vue` `onSearchShortcut`、`install-progress` `statusText`、`install-history` 三函数、`environment-versions` 两函数、`confirm/index.vue` install 回调、`scripts/check-i18n.ts` `checkClientLocales`（工具脚本）。

**.vue 模板豁免**：`install/index.vue`、`market.vue` 用模板内首元素上方的 HTML 注释（fallow 对模板的锚点在首元素行，多行标签场景注释无法插入标签内部）；`bundle-install/index.vue`、`package.vue`、`bundle-uninstall/index.vue` 因报点锚点在多行标签的属性行中间，登记到 `.fallowrc.jsonc` 的 `health.thresholdOverrides`（带中文 reason，保持 findings 可见、仅放宽阈值）。

### 5.4 health 分数结论（89.3，未达 90，差距分析）

- 实测口径：带 coverage（`fallow health --coverage coverage/coverage-final.json`）分数 **89.3**、critical **44 → 0**（目标 ≤15 大幅超额）、超阈函数 149 → 135。
- 计划书引用的基线 87 系旧缓存口径；干净重跑基线为 89.2，本轮真降复杂度后到 89.3。
- **分数模型特性**：fallow 的 maintainability 为固定校准，只认真实函数复杂度——拆文件（搬家）不涨分，`thresholdOverrides`/豁免抑制 findings 也不动分数（实测验证：登记前后 89.3 不变）。87→89.2 的提升来自 coverage 接入与既有治理，本轮 12+ 处真降复杂度函数因分摊在同文件内贡献有限。
- **剩余差距的本质**：70–85 分文件共 37 个，绝大多数是 §3.2 判定「登记豁免」的状态映射/对话框文件（`use-package-visibility` 76.8、`use-install-decision` 76.6、`use-package-card-meta` 76.5 等）；high 46 项中 43 项为 CRAP 型（CC 仅 7–9，由该函数覆盖率不足推高，补测试可消 findings 但不动分数）。要到 90 只能对状态映射做强行查表改造——违背 §7「不要为了降分而强行抽象」，故止步 89.3。
- **后人路径**：若要冲 90，按文件分数排序对 37 个 70–85 分文件逐一做真查表改造（接受可观察输出不变性验证成本），并同步补 43 个 CRAP 型函数的测试。

## 6. 验收标准（全部满足才收工）

- [x] `yarn check` 全绿（15 个大文件 ≤250 行，`types.ts`/`installer/index.ts` 登记不拆理由）
- [x] `yarn build` 成功，产物 `lib/` 与 `dist/index.js`/`dist/style.css` 存在且契约面不变（聚合出口原样转发，测试零改动全绿佐证）
- [x] `yarn test` 全绿（86 文件 1019 用例），覆盖率语句 **88.9%** ≥ 86.9% 底线（升 2 个百分点）
- [x] fallow critical **0** ≤ 15；health 89.3 < 90，差距分析与达成路径登记于 §5.4
- [x] §5 登记表填写完整
- [x] 提交信息简体中文，逐模块提交

## 7. 给执行者的灵活度说明（重要）

- **本计划书是方向不是死命令**：拆分边界、函数粒度、豁免判定都以你调研后的判断为准；计划书里的行数/方案是初稿，发现更好的拆分方式尽管改。
- **不要为了降分而强行抽象**：数据驱动映射（状态→文本/图标）拆函数没有收益时，登记豁免比硬重构更正确；`.fallow` 缓存旧了，一切以重跑结果为准。
- **契约面是红线**：DataService/RPC/广播/HTTP/命令/导出面绝不能动；不确定某导出是否被外部消费时，查 `docs/reference/前后端调用契约.md` 或保守 re-export。
- **小步提交**：每完成一个模块跑一次门禁并提交，方便回退与评审。
- **遇到 Biome JSON 行尾不可见字符噪音**：已知无害现象，跳过即可（见 DEVELOPMENT.md §6.12）。
